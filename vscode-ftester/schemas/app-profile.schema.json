{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ftester アプリケーションプロファイル",
  "description": "アプリケーションプロファイル(profiles/apps/<name>.json)。common → ios/android セクションの順で後勝ちマージされる(platformごとに common の値を上書きできる)。構造の正は Sources/FTCore/RunProfile.swift の AppProfile / AppProfileSection / ProfileResolver.validate。",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "common": {
      "$ref": "#/definitions/section",
      "description": "全プラットフォーム共通の既定値。"
    },
    "ios": {
      "$ref": "#/definitions/section",
      "description": "iOS 用の上書き値(common の値より優先される)。"
    },
    "android": {
      "$ref": "#/definitions/section",
      "description": "Android 用の上書き値(common の値より優先される)。"
    }
  },
  "definitions": {
    "section": {
      "type": "object",
      "description": "アプリケーションプロファイルの1セクション(AppProfileSection)。",
      "additionalProperties": true,
      "properties": {
        "appName": {
          "type": "string",
          "description": "ユーザーがアプリを識別するための表示名(レポート/GUI/ログで使用)。"
        },
        "app": {
          "type": "string",
          "description": "bundle identifier(iOS) / パッケージ名(Android)。実行プロファイルの解決に必須(common かプラットフォーム別セクションのいずれかに定義されていればよい)。"
        },
        "appPath": {
          "type": "string",
          "description": "パッケージファイル(.app / .apk)のパス。プロジェクトルート相対・絶対パス・~ 展開のいずれかに対応する。"
        },
        "autoInstall": {
          "type": "boolean",
          "description": "実行前に appPath を自動インストールするか(既定 true)。",
          "default": true
        }
      }
    }
  }
}
