// 画面が起動できること.swift
// generated by ftester explore v0.1 (apple-fm-on-device)
// goal: 画面が起動できること
// TODO: 探索が未完了のまま生成されています。手直ししてから実行してください

import FTDSL

@TestClass(app: "com.example.sampleapp", platform: "ios")
class 画面が起動できること {

    @Test("画面が起動できること")
    func S0010() {
        scenario {
            scene(1) {
                condition {
                    launchApp()
                }.action {
                    tap("#login_btn||ログイン||.Button")  // ログインボタンが存在するため、画面起動の第一歩としてタップする。
                }
            }
        }
    }
}
