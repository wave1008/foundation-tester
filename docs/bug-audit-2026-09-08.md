# バグ出し台帳(2026-09-08・60分耐久)

対象: HEAD ee46a169(作業ツリーに未コミット変更あり = `localDirty`。リモートランナー2機は HEAD で適合)。
手法: **フル E2E(5 SUT / iOS in-app + Android)を `--performance` 付きで連続3周 + 補充1周**、
3機フリート(手元 M2Ultra 8レーン + M1Ultra 4 + M1Max 2)。連続実行 3,305 秒(55分)、
事前のフリート起動を含めて約 61 分。`swift test --parallel` は耐久の前後とも緑(5,469 件)。

**「再現」= 実行して確認 / 「確認」= コードパスを読み切って確認**。以下は全て再現または確認済み。

集計: **78 run / 791 シナリオ / 失敗 6(0.76%)**。全 run が `performanceMode`、`measurementInvalid` は 0
(レーン数が動いていないので所要は計測に使える)。`workerAnomalies` は0件。

---

## 0. 修正状況(2026-09-09・未コミット)

**直したもの**(`swift test --parallel` 5,512 件緑):

- §1 FM 台帳の偽の生存 → 門の向きを反転(`FTCore.FMLedgerWriteRole`)。
  production の実行ファイル(`fleetest` / `fleetest-mcp` / `FTScenarioRunner`)が起動直後に
  `enableForProduction()` を呼んだときだけ本番の置き場を開く(fail-closed。呼び忘れは「不明」であって嘘ではない)。
  入口の集合は `FMLedgerWriteRoleWiringTests` がソース走査で固定。
  **`FMBreaker` の置き場も同じ門を通す** —— ここが最初の修正から漏れており、
  `FMHealthTests` が合成値で `ok: false` を3回叩いて**本番のブレーカを落としていた**
  (= §1 併発の 22:41 の犯人はこれ。同じスイート内の `FMGateWaitWiringTests` も
  `FMGate.enter` を短絡されて落ちていた)。opt-in の無いプロセスは pid ごとの一時パスへ隔離する
  (無効化はしない = ブレーカ自体を検証するテストは従来どおり動く)
- §4 `devices up` の全滅 exit 0 → `bootAll` が台ごとの成否を返し、
  `BootOutcomeSummarizer`(純粋関数)が要約。**部分失敗は従来どおり exit 0 + ⚠️ 1行**、
  **全滅だけ exit 1**。`api start-all-devices` / `api restart-devices` も全滅で `finished{ok:false}`
  (ワイヤの形は変えていないので `ProtocolVersion` は据え置き)
- §5 stale な AVD ロック → 早期終了時に emulator 自身のログの `FATAL`/`ERROR` 行をエラーへ載せ、
  多重起動の申告があり**かつその AVD を握るプロセスの実体が無い**ときだけ
  `hardware-qemu.ini.lock` を消して1回だけ再試行する(`multiinstance.lock` は消さない)。
  実体判定は `AndroidDataWiper.avdProcessPresent`(ps 走査)を共有 = pid で生死を見ない
- §6 `fmDead` が黙る → 台帳の観測が古い/無い経路だけ `FMBreaker.isOpen` の事実で dead を補う
  (観測済みの経路は上書きしない)。`docs/results-json.md` も同時に更新
- §7 ステップ内訳 → `TimelineStepRecord` に `snapshotMs` / `actionMs` / `waitMs` を Optional で追加
  (nil はキーごと出さない)。`docs/results-json.md` も同時に更新

**実デバイスで当てたもの**: §5 の自己修復は陽性対照で確認した ——
`Pixel_9_Android_15_-07.avd` に**生きているが emulator ではない pid** を書いた
`hardware-qemu.ini.lock` を仕込む(実際に壊れたときと同じ形)→ `api start-device` が
`found a stale hardware-qemu.ini.lock with no emulator process holding it — removed it and retrying the boot once`
を出して起動に成功。`devices up` の正常系(全台起動済み)は ✅ / exit 0 のまま。

- §3 アサーションの青天井 → **期限切れ後の「もう1枚」を予算で止める**
  (`FTCore.SlowSnapshotBudget.mayRetake(stepElapsedMs:lastSnapshotMs:commandTimeoutMs:)` = 純粋関数。
  直前の snapshot の実所要を「次も同じくらいかかる」と見積もり、`FTSync.commandTimeout` の
  外枠に収まらなければ撃たない)。外枠は FTCore から参照できないので **FTRuntime が渡す**
  (nil = 外枠を持たない呼び出し元 = MCP 等は従来どおり)。併せて **1枚の snapshot が
  そのステップの待ち予算を超えたら `StepNote.slowSnapshot`**(判定は変えず注記だけ)。
  同型のアサーションループ8本すべてに入れた(対象外2件は理由つきで据え置き)。
  45秒級の snapshot は狙って起こせないので**注入口** `FT_FAKE_SNAPSHOT_DELAY_MS` を置いた
  (`FrozenInjection` と同じ思想。観測だけを変えデバイスには触らない)

**実デバイスで当てたもの(2)**: §3 は注入口で対照を取った(E2E-Flutter/android・実在しない要素を待つ witness)。
**注入なし = 5.80 秒で not-found**(既定待ち5秒+取り直し1回)。
**1枚 65 秒を注入 = 69.3 秒で not-found**(`failureKind: not-found`・注記 `slow-snapshot`・
`snapshotMs: 69334` = 所要の全量が snapshot だったことが記録だけで分かる)。
**120 秒のコマンド上限には達しない** —— 従来はここで 65+65 の取り直しに入り、
`failureKind: timeout` の無情報な赤になっていた。

**残り**: §3 の「冷えたフリートで実際に 45 秒級が出る条件」での確認は未実施
(注入口で機構は確かめたが、実物の遅さで踏むのは次のフル E2E の1周目が自然な機会)。
§4 の同型として `api stop-all-devices` / `devices down` にも同じ形があるが、
停止側は「個別の失敗で全体を止めない」が意図的な仕様なので手を付けていない(要判断)。

---

## 0.1 性能(冷 vs 温)

プロファイルごとの壁時計(同じ runGroup の最初の開始→最後の終了)。

| project/profile | 1周目(冷) | 2周目 | 3周目 |
|---|---|---|---|
| E2E-CMP/ios-inapp | 269s | 87s | 82s |
| E2E-RN/ios-inapp | 194s | 88s | 82s |
| E2E-Flutter/ios-inapp | 184s | 95s | 84s |
| E2E-CMP/android | 184s | 64s | 66s |
| E2E-iOS/ios-inapp | 166s | 80s | 78s |
| E2E-Android/android | 106s | 103s | 62s |
| E2E-RN/android | 80s | 59s | 51s |
| E2E-Flutter/android | 63s | 66s | 60s |
| **合計** | **1246s** | **642s** | **565s** |

スイート全体は 1507 → 818 → 742 秒。**冷えたフリートの1周目だけが桁違いに遅く(iOS 系 −48〜−68%)、
赤もそこに集中する**。10秒超のステップは 36 → 8 → 3 件(最大 118.7 → 57.7 → 14.1 秒)。

**読み方**: 性能や flake を run 横断で比べるときは**1周目を捨てて2周目以降で比べる**。
1周目の赤を退行と読まない(HEAD 対照を取る前に、温めた2周目で再現するか見る)。

---

## 1. `swift test` が本番の FM 台帳へ偽の「生存」を書く【最大】

**再現**(2026-09-08 23:49、手順そのまま):

```bash
rm -f ~/.fleetest/fm-liveness.json ~/.fleetest/fm-usage/*.json
swift test --parallel --filter FMGateWaitWiringTests     # テスト2本だけ
cat ~/.fleetest/fm-liveness.json
# → {"text":{"state":"alive","source":"call","ms":1,"checkedAt":...}}
ls ~/.fleetest/fm-usage/                                  # → <pid>.json が新規に作られている
```

同時刻の実態は **FM 100% 死**(`fleetest doctor --fm-only` が両経路 ❌ で exit 1)。

**出どころ**: `Tests/FTCoreTests/FMGateWaitWiringTests.swift` の
`FMHealth.record(kind: "test", path: .text, ms: 1, ok: true)` = **合成値**(FM は1度も呼んでいない)。

**なぜ門が効かないか**: `FMLiveness.writeURL` と `FMUsageLedger` の書き込み先はどちらも
`XCTestConfigurationFilePath` の有無でテストプロセスを弾く設計だが、
**`swift test --parallel` のワーカープロセスではこの環境変数が立たない**。

**害**(この台帳を読む4つが全部騙される): モニターの FM 行 / `ProfileRunner.warnIfFMDegraded` の
run 前警告 / `run.json` の `fmDead`(FM が死んだ run の緑を仕分けるための欄)/ `ft_status`・`ft_doctor`。
`FMLiveness.swift` 冒頭のコメントは「FM を1回も呼んでいないのに**死んでいる**が配られる」を警戒しているが、
実際に起きたのは**偽の生存**で、規律③(availability を書き手にしない。`unavailable` の向きだけは
信じてよい)と同じ理由でこちらのほうが害が大きい。

**併発**: 22:41 に本番ブレーカが落ち(`~/.fleetest/fm-usage/<pid>.json` に 3呼び出し/3失敗の記録)、
耐久1周目は cooldown 600 秒のあいだ FM 無しで走った
(シナリオの errorLogs に `Skipped 2 FM call(s) (circuit breaker open ...)`)。

---

## 2. 手元 Mac の FM が 100% 失敗へ(既知フラップの再発)【大・環境】

- 22:41(耐久前): `doctor --fm-only` は text/vision とも **実呼び出しで available**
- 耐久中: 手元 LDIPC96 の実呼び出し **25回中15回失敗(60%)**。
  リモートは **0%**(M1Ultra 4回 / M1Max 9回)= **この Mac 固有**
- 23:43(耐久後・`doctor --fm-load`): **text 610/610・vision 294/294 が失敗(100%)**
  - text: `com.apple.SensitiveContentAnalysisML(15)` ← `ModelManagerServices.ModelManagerError(1001)`
  - vision: `LanguageModelError(-1)` ← 同 `ModelManagerError(1001)`
- **署名は既知のフラップと同一**(根本原因は ANE 0x10006 で安全性モデルがロード不能。
  2026-07-23 に特定済み・2026-09-06 の台帳にも同じ環境メモがある)。**新しい事象ではない**。

**効き目**: 2・3周目の全緑は、**8/14 レーンを持つ機械で occlusion-guard が使えない状態**で出た緑。
ただし実際に素通りしたのは `guardSkipped` 42 件 / ガード通過 6,793 件で、
大半は OCR・型・ラベルの足切りで FM を要さないため影響は限定的。
一方 heal / triage / screenLooksLike は失敗時にしか効かないので、
**この状態で赤が出ていたら自己修復もトリアージも無かった**。

**対処**: FM に依存する検証の前に `fleetest doctor --fm-only` を撃つ。死んでいたら Mac を再起動してから測り直す。

---

## 3. 既定待ち5秒のアサーションが 30〜113 秒かかり、120秒を跨いだ回だけ赤になる【大】

**再現**(1周目 E2E-CMP/ios-inapp): 6シナリオが `select` の
`the command timed out (120s)`(`failureKind: timeout`)で失敗。全て手元の8レーン、
リモート2機は0失敗。窓は 22:48:29〜22:50:57 で、**in-app ブリッジ8本を供給していた時間帯**
(`.fleetest/bridge-813*.inapp` の作成時刻 22:48:29〜22:50:04)と一致する。

失敗時の要素一覧には**探していた要素がそのまま写っている**
(例: `[4] staticText "selected=row_40" id=txt_row_selected`)= 要素は実在し、木が引けなかっただけ。

**素性**: 検証コマンドの既定待ちは `FlowStep.defaultWaitSeconds = 5` 秒。ところが
`StepExecutor+Assert` のポーリングは**期限を周回の合間でしか見ない**ので、1回の
`driver.snapshot()`(`BridgeClient.Timeout.session` = 45 秒)が伸びると 5 秒の予算を無視して
30〜100 秒進む。同じ窓で `select "#txt_row_selected"` が **104.9 秒かかって成功**した回もある
(同一症状の生存例)。**120 秒のコマンド上限を跨いだ回だけが赤**になるので、**赤は運次第**で、
同じ事象の大半は「緑だが 100 秒かかった」として黙って通っている。

FM のゲート待ちは原因ではない(この run の `fm.gateWait*` は 0)。

**記録の穴**: 残るのは `failureKind: timeout` と "the command timed out (120s)" **だけ**で、
「ブリッジが答えなかった」という事実が `notes` に載らない。今回は
ポートの割り当てとファイルの作成時刻から外形的に再構成するしかなかった。

**2・3周目では再現しない**(温まったフリートでは0件)。

---

## 4. `devices up` は1台も起動できなくても exit 0 で「✅ 完了」と言う【中】

**再現**: `fleetest devices up --project E2E-CMP --profile android` が
`❌ Pixel 9(Android 15)-03: the emulator exited before it registered with adb (status 1; ...)` を出したのに、
最終行は `✅ Device start-up sequence complete`・**exit code 0**。

**素性**: `DeviceBooter.bootItem` が `catch { log("❌ …") }` で握り、`bootAll` は Void を返し、
`DevicesCommand.Up.run` は無条件に ✅ を出す。

**同型(掃討)**: `ApiStartAllDevicesCommand` / `ApiRestartDevicesCommand` も個別の失敗を握って
`finished{ok:true}` を出す(後者の abstract は "exit code 1 when ok:false" と書いてあるが、
実際に ok:false になるのは device-not-found とプロファイル読み込み失敗だけ)。

規律「1台の失敗で全体を落とさない = **全滅のときだけ throw**」に照らすと、**全滅でも成功**は逸脱。
拡張側には「親の finished は ok:true のまま来るので中継された子の finished を見る」という
既知の注意書きが `vscode-fleetest/src/monitorDeviceLifecycle.ts` にあり、
**同じ穴の機械単位版は既に踏まれている**(今回はその台単位版)。

---

## 5. stale な AVD ロックでレーンが恒久的に死ぬ【中】

**再現**: `~/.android/avd/Pixel_9_Android_15_-03.avd/hardware-qemu.ini.lock` が前日から残存
(中身は pid 651。**その pid は再利用されて無関係の `cloudd` になっていた**)。
emulator は起動を拒否し、ログ(`~/Library/Logs/fleetest/emulator/<avd>.log`。**ツール自身が書いている**)に

```
FATAL | Running multiple emulators with the same AVD is an experimental feature.Please use -read-only flag ...
```

ところがツールの発話は `... Check the AVD name and <log>` で、**AVD 名は正しい**ので誤誘導。
手で lock を消したら即起動した(qemu プロセスは1つも無かった)。

**直す形**: 早期終了の経路で自分が書いたログの `FATAL`/`ERROR` 行をエラーに載せる。
多重起動の申告があり、**かつその AVD を握っているプロセスの実体が無い**ときだけ
`hardware-qemu.ini.lock` を消して1回だけ再試行する(`multiinstance.lock` は消さない)。
**生死は pid で見ない** —— 今回まさに pid 再利用で壊れた。
リポジトリの規律「台帳はプロセスの実体で掃除する」と同じ。

---

## 6. FM がブレーカで死んでいる run ほど `fmDead` が記録されない【中】

1周目の run は開始時点で FM が死んでいた(シナリオの errorLogs に
`Skipped 2 FM call(s) (circuit breaker open ...)`)のに、`run.json` の **`fmDead` は欄ごと不在**、
`scenarios/*.json` の `fm` は null。一方、run の**最中に**死んだ7本では正しく記録された。

**素性**: `RunRecorder` は閉じるとき `FMLiveness.current()`(鮮度 120 秒)を読む。
**ブレーカで落ちている間は誰も FM を呼ばない = 台帳が更新されない**ので、run が数分続くだけで
観測が古くなり「不明(欄ごと省略)」に落ちる。
`FMBreaker.isOpen` は mtime を1回読むだけの確実な事実なのに使っていない。
**「FM が死んだ run の緑」を仕分けるための欄が、まさにその場合にだけ黙る**。

---

## 7. ステップの内訳(snapshotMs / actionMs / waitMs)が結果 JSON に残らない【小】

`StepExecutor` は `StepTiming(durationMs:snapshotMs:actionMs:waitMs:)` を計測して
`ScenarioEvent` まで運んでいるのに、`RunRecord.TimelineStepRecord` は `durationMs` しか持たない。
§3 の「104 秒のうち何秒が snapshot か」を記録から言えない。
1ステップ3整数を足すだけで、遅いステップの仕分けが推測から観測になる。

---

## 8. 手法のメモ(次に同じことをやる人向け)

- **`--performance` を全周に付ける**とレーン数の変動が `measurementInvalid` で分かる(今回は0件 = 全周が計測に使える)
- **1周目は冷えたフリートの費用を含む**ので、比較は2周目以降で行う
- **同一 run 内に手元とリモートが居る構成は、機械固有の事象の対照がその場で取れる**
  (今回は FM の 60% 失敗が手元だけであることを、リモート2機の 0% と並べて即断できた)
- 遅いステップは `scenarios/*.json` の `timeline[].durationMs` を run 開始からの経過で並べると窓が見える
