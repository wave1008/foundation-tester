# MCP 監査ラウンドの回し方(深さでなく幅)

実アプリを触って MCP(`ft_*`)の欠陥を拾い、直す作業の手順書。
**1ラウンド = 初見の「形」1つ**(アプリ1本ではない。次節)。

## 何を1ラウンドと数えるか —— 軸は2本

**軸① 画面の形**(木がどう出るか): 本文 / 格子 / 深いリスト / フォーム / モーダル /
全画面キャンバス / 無限スクロール / タブ …

**軸② セッションの形**(サーバが何を覚えるか): 1台1アプリ / **2台以上** / **OS 跨ぎ** /
アプリ切替 / 長時間 / ブリッジの建て直しを挟む …

**アプリ名は軸ではない。** 2026-08-13 にこの数え方へ直した。それまで「1ラウンド = 初見のアプリ
1本」と数えていたため、**ブラウザ6ラウンドが全部「天気」で、うち5回は同じ格子**という状態を
台帳が検出できなかった(アプリ名は tenki.jp / Yahoo!天気 / 気象庁 と変わっていたので
「初見」に見えていた)。6ラウンド目に至っては **2・3ラウンド目と同じ tenki.jp を再訪**し、
木まわりで拾ったものは全部が既記録の再発見だった。

## なぜ深さでなく幅か

同じ対象(地図)を繰り返した 19 ラウンドの収穫:

| ラウンド | 実バグ |
|---|---|
| 1(2026-08-07・初回) | 12 |
| 2〜3 | 9 → 6 |
| 4〜7 | 4 → 0 |
| 8〜19(08-10〜08-12) | **0〜2/回**(0 の回が4回) |

**収穫は対象の新しさに比例し、深さには比例しない。** 初見の対象は毎回まとまって出た
(sut-ec-mobile の in-app 初監査で4件 / React Native 対応で固有の罠 / 2026-08-12 に
設定・チャット・ブラウザをコーパスへ足しただけで、注記の汎用性判定が3本ひっくり返り、
実 web ページで既知型の誤検知が 10 件出た)。一方、19 ラウンド目の地図から出るのは
ほぼ言い回しだけになった。

**同じ対象でも「画面の形」が変われば初見と同じだけ出る**(2026-08-12・ブラウザ3ラウンド目)。
1・2ラウンド目は**縦に流れる本文**を読ませていて、実バグは要素上限と a11y の取りこぼしだった。
3ラウンド目に**格子(週間表)**を読ませたら実バグ4件 —— 見出し行の欠落・空振りの偽成功・
`scrollFrameRect` の見落とし・codegen の書き戻し漏れ。

## 自作の機構を監査しているとき、それは収穫ではない

**入れたばかりの検知は、次のページで誤検知を出す。** 3ラウンド目に入れた
`gridWithoutHeaderNote` は固定コーパス30枚で誤検知0だったが、**次のラウンドで触った1ページで
2件出した**。30枚の「誤検知0」は**その30枚に無い形については何も言っていない**。

これは観察としては正しいが、**美点として運用すると原理的に終わらない**。直近2ラウンドの
「実バグ」を出自で分けると:

| ラウンド | 自作機構の欠陥 | 元からあった欠陥 |
|---|---|---|
| browser 4 | ⒜ `gridWithoutHeaderNote`(3で新設)/ ⒝ `webViewGapNote`(2で新設)/ ⒞ `gridMinRows`(3) | ⒟ `ft_scroll_to` の注記落ち |
| browser 5 | ⒜ `webViewGapNote`+`emptyTreeNote` が測れない / ⒝ `looksUnchanged`(15 の waitForChange 由来)/ ⒟ ⒜と同じ指標 | ⒞ 横スクロールの複製 |

**3/4 が自作機構の欠陥。** 1本足すと次のラウンドが1本確定し、そのラウンドでまた足すので、
実バグの本数は減らないまま注記だけが増える(ブラウザは 3→3→4→4→4 で**減衰していない**。
同じ期間に `NoteCatalog` は 17 本になった)。

**だから増設と検分は同じラウンドでやらない**(手順の 5.5)。

## 台帳が数えていなかった軸(2026-08-13 追加)

木の形とは独立に、**「2台以上を触ったセッション」でだけ出る欠陥**がある:

- ラウンド19: `serial` だけの呼び出しが黙って iOS へ
- ラウンド20: キャッシュ命中でセッション記憶が更新されない
- 2026-08-13: 失敗キャッシュの再生がライブの失敗と見分けられない / 宛先記憶が曖昧なまま適用

3件とも、天気タスクが「**両OSで**」だったから**副産物として**露出した。狙って触った回は一度も無い。
軸②はこれを狙うためにある。

**地図(Google マップ / Apple マップ)の反復監査は 2026-08-12 に閉じた。**
再開するのは、地図に固有の新しい経路(新しい画面・新しい操作)を触るときだけ。
「赤羽→立川をもう一周する」はしない。

## ラウンドの手順

1. **形を選ぶ**(下の台帳の「未着手」から1つ)。条件は**軸①か軸②のどちらかが初見**であること。
   **アプリが初見でも、形が既出なら初見ではない**(ブラウザで別の天気サイトを開くのは同じ形)
2. **タスクを1つ決めてから触る**(「探索する」ではなく「◯◯を達成する」)。
   目的の無い探索は画面を並べるだけで、詰まりが観測できない
3. **触る。詰まったところをその場で記録する** —— 何を撃って、何が返って、なぜ止まったか
4. **拾ったものを3つに分ける**(下の節)
5. **バグを直す**。同じ型が他に無いかを機械的に掃討する(CLAUDE.md の規律)
5.5. **このラウンドが「検分の回」なら、新しい注記を足さない**(下の交互規則)
6. **フィクスチャを1枚採る**(`Tests/Fixtures/RealAppSnapshots/`)。
   `NoteCoverageTests.archetypes` に分類を足し、`SweepHarnessTests` の基準値は
   **検知を1件ずつ検分してから**書く
7. **台帳へ1行足す**(形・セッションの形・出自の内訳を書く)

### 増設と検分は交互にやる

前のラウンドで**新しい注記・新しい検知を入れたら、次のラウンドは検分の回**にする。
検分の回にやってよいのは **誤検知の修正・条件の絞り込み・削除**だけで、**新しい注記は足さない**。
拾った増設案は台帳に積んで次の増設の回へ回す。

これが無いと、1本足すたびに次のラウンドが1本確定し、そのラウンドでまた足すので終わらない
(上の「自作の機構を監査しているとき」)。**注記の本数は `NoteBudgetTests` が固定している**ので、
増やす回には根拠(手数の差 or 実機 witness + コーパス誤検知0)を台帳へ書くことになる。

## 拾ったものを3つに分ける

| 種類 | 例 | 判定の仕方 |
|---|---|---|
| **バグ** | 別の要素を撃った / 成功と言って届いていない / 画面の値を誤って報告した | その場で直す。再現テストを足す |
| **自作機構の欠陥** | 前のラウンドで入れた注記が誤検知する / 測れない形がある | その場で直すが、**収穫として数えない**(台帳の内訳へ「自作」と書く) |
| **言い回し・注記** | 「もっと分かりやすく言えたはず」「この注記は誤解を招く」 | **その場で直さない**。`Scripts/mcp-bench.sh` の手数が動くかで決める |

**この分離がこの手順書の要点。** バグは有限なので減衰するが、言い回しの改善案は無限に出る
(評価者が読んで違和感を探す限り、必ず何か出る)。分離せずに回すと、収束するのは
バグだけで、注記と分岐は単調に増え続ける —— 実際そうなった(docs/verification.md
§「注記は『読んだ印象』でなく『まっさらなエージェントの手数』で足し引きする」)。

**2列目を分けたのは 2026-08-13。** 「実バグ4件」と書き続けていたが、出自で割ると
3件が自作機構だった。混ぜて数えると**収穫が減っていないように見えて、実際は自分の尾を
追っている**ことに台帳が気付けない。

## 停止規則

- **1ラウンドの終わり**: 決めたタスクを完遂したら終わり。「ついでにもう1画面」はしない
- **その形の終わり**: **同じ形で、元からあった欠陥が0のラウンドが2回続いたら閉じる**。
  数えるのは1列目だけ —— **自作機構の欠陥は0の判定に入れない**(入れると、自分で入れた
  注記の手直しが「まだ収穫がある」の証拠になり、永久に閉じられない)
- **全体の終わり**: 無い。台帳の未着手が尽きたら、そのとき残っている形の空きを埋める

## 台帳①: 画面の形

**ラウンド数は形で数える**(アプリ名では数えない。上の「何を1ラウンドと数えるか」)。

| 形 | 触った対象 | ラウンド | 状態 |
|---|---|---|---|
| 地図 / 高密度キャンバス | Google・Apple マップ | 19 | **閉じた**(2026-08-12) |
| ブラウザ・**格子**(表を読む) | tenki.jp ×3・Yahoo!天気・気象庁 | **5** | **閉じる直前**。元からあった欠陥は直近2回とも**1件**(browser 4 の⒟・5 の⒞)。次に0が2回続けば閉じる。**同じ形をこれ以上サイトだけ替えて回さない** |
| ブラウザ・本文(縦に流れる記事) | weather.yahoo / tenki.jp | 1 | 開いている(改行入りラベル・`ft_type` の助言・掃討出力の欠落) |
| 深いリスト(EC) | sut-ec-mobile(iOS in-app) | 1 | 開いている(4件・in-app エンジンの初監査) |
| E2E SUT 5種 | CMP/iOS/Android/Flutter/RN | 常時(`Scripts/e2e.sh`) | 監査ではなく回帰。**実アプリの形は代表しない** |
| 設定ツリー | iOS 設定 / Android 設定 | **1**(2026-08-13) | 開いている。**元からあった欠陥1件**(下記)。iOS は行を `clickable` の容器で包む形が全画面に出る |
| チャット | iOS メッセージ / Android メッセージ | **2**(2026-08-13) | 開いている。**元からあった欠陥は 1 → 1**(1回目=注記の偽陽性・**決着済み**/ 2回目=`ft_type` が入力後の値を誤って報告。下記)。入力と読み返し・キーボード遮蔽・id 代替の提示はすべて正しく動いた |
| メディア | iOS 写真 | 0 | **未着手**(コーパスに1枚) |
| 実 web ページ(オーバーレイ) | Safari | 0 | **未着手**(コーパスに1枚。**overlay の誤検知 10 件を検分済み** = 触れば出る) |
| **フォーム / ログイン** | — | 0 | **未着手**(入力・検証エラー・キーボード遮蔽が重なる形。bench の `cmp-text-input` は対照のみ) |
| **モーダル / ダイアログ** | — | 0 | **未着手**(背景を a11y から落とす形。`and-overflow` が 56.4% 未表現の witness) |
| **無限スクロール** | — | 0 | **未着手**(読むたびに木が変わる = `looksUnchanged`・ref 世代の的) |
| キーパッド | Android 電話 | 0 | **未着手**(コーパスに1枚) |
| **タブ切替** | — | 0 | **未着手**(同じ `#id` が別画面に居る形) |

## 台帳②: セッションの形(2026-08-13 新設)

木の形とは独立の軸。**ここを狙って触った回はまだ無い** —— 下の3件はすべて別の目的の
ラウンドで副産物として出た。

| セッションの形 | ラウンド | 状態 |
|---|---|---|
| 1台・1アプリ | 25(既定) | 実質すべてのラウンド |
| **2台以上(同一 OS)** | **1**(2026-08-13) | 開いている。**元からあった欠陥1件**(下記)。乗り物は E2EAppCMP —— 契約が固定された最も使い込まれた盤面をあえて選び、**驚きがあれば必ずセッション軸由来**だと言えるようにした |
| **OS 跨ぎ** | **1**(2026-08-13) | 開いている。**元からあった欠陥1件**(下記)。木・ref・宛先の取り違えは1つも無く、**失敗と記憶の相互作用**でだけ出た |
| **アプリ切替**(同一機で launch を跨ぐ) | **1**(2026-08-13) | 開いている。**元からあった欠陥1件**(下記)。`switchedAppNote`(back でアプリを出た形)と `ft_open_url` の既定の開示は**どちらも正しく発火した** |
| **ブリッジの建て直しを挟む** | **1**(2026-08-13) | 開いている。**元からあった欠陥1件**(下記)。狙いどおり、**入れたばかりの防御が発火しないこと**を掴んだ |
| **長時間**(失敗キャッシュ・TTL を跨ぐ) | 0 | **未着手**。副産物で出た欠陥: 60秒失敗キャッシュの再生が見分けられない(2026-08-13・修正済み) |

**持ち越しの候補**(証拠が無いのでまだ触らない):

- `missingPageContentNote` は `addressBarCandidate`(`URL` / `TabBarItemTitle` / `url_bar` の
  identifier)だけを根拠にしており、**そういう identifier を持つネイティブ画面**で
  ブラウザ向けの文言が出得る(2026-08-13 のレビュー指摘)。ただし**witness がまだ無い** ——
  コーパスで 0.564 に達する地図画面はアドレス欄が無いので黙る。出力がずれるだけの型なので、
  実例が出るまで条件を足さない(足すと今度は真陽性を削る危険がある)

**次にやる順**(2026-08-13 更新。上から):

1. **長時間**(台帳②の最後の未着手)—— TTL・失敗キャッシュ・ブリッジの寿命を跨ぐ形。
   OS 跨ぎのラウンドで**木も ref も宛先も無傷だったのに、「失敗の後始末」で1件出た**ので、
   セッション軸の残りは**時間とエラーの相互作用**に賭ける価値がある。
   例:「失敗を挟んで数分待ってから省略呼び出しをする」「TTL 切れの後に古い ref を撃つ」
2. **ブラウザ・格子を閉じる**(サイトを替えて1周ではなく、**閉じる判定のためだけ**に回す)
3. **フォーム / ログイン**(コーパスにも無い形)
4. **モーダル / ダイアログ**

**セッション軸で分かったこと**(4ラウンドぶん): **木・ref・宛先の取り違えはもう出ない**
(2台以上・アプリ切替・建て直し・OS 跨ぎで入れた4つのガードが効いている)。
出るのは**ガードそのものが黙る条件** —— 記憶が空・キャッシュ命中・失敗の後始末。
**次に狙うならガードの前提を壊す形**であって、新しい画面ではない。

**その先の候補**(コーパスにも無い形。1本ずつ足す):

- **メディア再生**(YouTube Music / 動画プレイヤー)—— 全画面のキャンバス + 重なる操作子
- **カレンダー / 表**(Google カレンダー)—— 同形セルの格子。地図のピンと似て非なる形
- **メール**(Gmail)—— 深いリスト + スワイプ操作 + 折りたたみ
- **地図以外の canvas**(ペイント・ゲーム)—— a11y がほぼ空の画面で MCP が何を言うか

**天気サイトはもう足さない。** 6ラウンド触った(うち5回は格子)。次に天気を開くのは、
`ft_scroll_to` の `maxElements` のように**据え置きにした項目を検分するとき**だけ。

## 各ラウンドの記録(画面の形)

| 記録 | 形 | ラウンド | 内容 |
|---|---|---|---|
| Android メッセージ(bench 盤面の作成を兼ねる) | チャット | 2(2026-08-13) | **タスク**: 新しい会話の画面で宛先に電話番号を入れ、候補を読んでから「戻る」で一覧へ帰る(`Bench/tasks/and-chat-newconv.json` として固定した)。**元からあった欠陥1件**: **`ft_type` が入力後の値を誤って報告する**。宛先欄 `#ContactSearchField` は空のときヒント文字列を `value` に載せる(`isShowingHintText()` が false なのでブリッジは `placeholder` を出さない)ため、`ft_type` が「the field already held "名前、電話番号、メールアドレスのいずれかを入力"; ft_type appends, so it now reads "名前、…5551234567"」と返す —— **同じ応答が返す木が `value="5551234567"` でその場で否定している**。原因は**連結後の値を予告していた**こと(replace 側は 2026-08-12 に読み返しへ直したのに、追記側だけ予告のまま残っていた=掃討漏れ)。修正は `appendVerificationNote` で**読み返した値だけを名乗る**(撃った文字だけが残っていたら「撃つ前の値は実在の内容ではなかった」として黙る)。読みは `snapshotAfter` の1枚に相乗りするので、その呼び方では往復が増えない。併せて**撃つ前の値の判定を DSL と共有**した(`TypeReadback.normalizedValue`。MCP だけが素の `value` を見ていたので、`placeholder` と同値の空欄でも MCP だけ警告していた)。**掃討①(同じ嘘を DSL も言っていた・witness あり)**: `StepExecutor` の同名注記も連結を予告していた。**自分の回帰スイートが毎回この嘘を印刷していた** —— `E2E-CMP` の `#field_single` は **Android では** value="単一行"(ヒント)/ placeholder なしなので、注記は `"単一行hello123"` を予告するのに、**同じシナリオの次の行 `textIs "#txt_echo_length" == "len=8"` が通っている**(11文字ではなく8文字)。実行結果を profile で割ると **android の run だけ 6 シナリオで発火・iOS は in-app も xcuitest も 0**(iOS の CMP は placeholder を**ラベル**として出すので `priorValue` が空)。ここでは読み返さない方針なので、予告をやめて「撃つ前に入っていた値」だけを言う形にした。**掃討②(同型・ただし再現していない)**: DSL は同じ前提で `verifyTypedText(expected: 撃つ前の値 + 本文)` を組むので、`plan` が必ず `.unverifiable` に落ち、**追送も打ち直しも走らないまま受理される**(読み返しの砦が丸ごと外れる)。**この退化は witness が無い** —— ヒントが value に載ると分かっている2盤面(Android CMP / Google メッセージ)はどちらも `verifiesTypedText == true` でこの経路を通らず、唯一 `false` の iOS in-app で当たる盤面はまだ見つかっていない。**失敗モードが沈黙なので witness 無しでも塞ぐ**(CLAUDE.md の規律。明記した)。実装は `StepExecutor.readbackTarget`(純粋関数)——「expected が `.unverifiable` のときだけ、撃った文字だけを目標に採り直す」。**順序を入れ替えないこと**が砦で、`typedOnly` を先に見ると prior と本文が同じ欄(prior="abc" に "abc" を追記)で**追記が届かなかった失敗が `.done` に見える**。**掃討③**: `ft_clear_input` も無条件に `cleared` と返していた(in-app iOS の UIKit 経路は clearInput の成否を検証なしで YES と返す = 2026-08-12 に replace 側で塞いだのと同じ穴)。読み返して `clearInput sent (cleared the field)` / 残存なら警告に分けた。**実機の陽性対照**(新しいプロセスで stdin から): 偽警告は消え、本当に値が入っている2回目は `already held "5551234567" … now reads "555123456799"` と**読み返した値**で警告する。変異 6/6 検出 |
| iOS 設定 / Android 設定 | 設定ツリー | 1(2026-08-13) | **タスク**: 検索または階層をたどってキーボード関連の設定へ到達し、状態を読む(両OS)。**元からあった欠陥1件**: **`unlabeledClickablesNote` が、同じ矩形に `#id` があるのに「安定したセレクタで指せない」と報告する**。iOS の設定は行を `clickable` の容器で包み、**その中に同一矩形の `button` + `#id`** を置くため、容器のほうが「ラベルも id も無い」に該当していた。実測でホーム画面 11 件・一般 20 件が該当し、注記が勧めるのは `#…collectionView >> .clickable[3]`(**兄弟の数で壊れる索引記法**)。同じ矩形に `#com.apple.settings.general` があるので、**注記の前提(「書けない」)自体が偽**だった。同一矩形に `.stable` なセレクタを持つ要素が居るときは黙る(`stableSelectorFrameKeys`)。**実装で1回外した**: 素の `selector(for:)` は索引記法も返すので、**無ラベル clickable 自身が「書ける」に該当して自分を twin と数え**、コーパスの真陽性(`ios-home` / `ios-maps_route_options`)まで消した —— レビューが `duplicateRegionNote` で指摘した自己一致と同じ型を、別の場所で再演した。`.stable` に限ることで解消。**検算**: フィクスチャを独立に走査して「消える画面は全件が同一矩形に安定セレクタを持つ」ことを確かめてから基準値を更新(`ios-profile` 4/4・`ios-settings_root` 11/11 が該当 / `ios-home` 0/1・`ios-maps_route_options` 3/11 なので**引き続き発火**)。実機で 20 件 → 1 件(残る1件は twin の矩形がインセットで異なる真陽性)。注記バイトは 5105 → 3150。**Android には出ない**(行の `clickable` 自身がラベルを持つ)。**新しい注記は足していない**(交互規則の検分の回) |

### 決着した持ち越し(測って「足さない」と決めた)

**`unlabeledClickablesNote` の「矩形の包含」形には規則を足さない**(2026-08-13 に計測して決着)。

観測はこうだった —— Android メッセージの「新しい会話」画面で `[16] clickable (12,85 126x126)` が
「安定したセレクタで指せない」と報告されるが、**その内側に一意ラベル `戻る`(43,116 63x63)**
があり、**実機で `tap '戻る'` が成功する**(= 注記の主張は偽)。同日の修正は**矩形の完全一致**しか
見ないのでここには効かない。一般規則の設計も難しく(「内側に `.stable` が1つだけなら黙る」は
iOS 設定の行を壊す・面積比で切ると Android の戻るボタン 25% が漏れる)、保留していた。

**測った**(Bench/measurements.md §2026-08-13。`and-chat-newconv` = この注記が発火する盤面 /
`and-settings-keyboard` = 対照。`--repeat 5`)。結果は **tools ±0** —— **注記を丸ごと落としても
手数が1手も動かない**。陽性対照も取れている(base では 1 run に4回出ており、variant では0回)。
偽の主張をしている盤面ですら効いていないのだから、**正しくして得られる手数は 0 以下**。

**この決着の型を覚えておく**: 保留の理由は「規則の設計が難しい」だったが、測ってみると
**そもそも設計する価値が無かった**。難しさで保留すると、いつか腕が上がったら着手する話に
見えてしまう。**設計の難易度ではなく効果で閉じる**。

**消す判断は別**(未着手): 測ったのは2盤面だけで、`ios-home` / `ios-maps_route_options` では
真陽性として出ている(`SweepHarnessTests`)。消すならその2形にも手数タスクが要る。

## 各ラウンドの記録(セッションの形)

| 記録 | 形 | ラウンド | 内容 |
|---|---|---|---|
| E2EAppCMP を iOS 1台 + Android 1台 | OS 跨ぎ | 1(2026-08-13) | **タスク**: 両 OS に同じアプリを入れ、iOS を `result=item2`・Android を `result=item3` まで進めて、最後に両方から各自の値を読む(`#id`・ラベルが両 OS で同一契約なので、誤配送は**もっともらしく成功する**)。**まず 0 件だった** —— ref は OS を跨いでも一度も衝突せず(iOS 1〜29 / Android 30〜)、宛先なしの呼び出しは「This session has also driven iOS」と名指しして拒否、iOS の ref を Android へ撃つと発射前に拒否、最後の読みも取り違え無し。**そこで「失敗×記憶」を突いて1件出た**: **失敗した宛先が、直前まで使えていた宛先の記憶を壊す**。実測 —— ①port 8138 を明示して成功 → ②存在しない port 8999 を明示して失敗 → ③**宛先を省いた呼び出しが、8138 でも 8999 でもない別のシミュレータ**(ホーム画面が返った)へ黙って行った。機構は「記憶は**解決時**に書かれる」こと: ② が 8138 を 8999 で上書きし、失敗の後始末(`forgetConnection`)が 8999 を消して**記憶を空にする**。空になると「まだ1台も指していない新しいセッション」と同じ扱いになり、`resolveIOSPort(explicit: nil)` = **ブリッジ探索**へ落ちて、このセッションが一度も名指ししていない機を拾う。**曖昧さのガードは記憶を見るので、記憶が空では原理的に働かない**(前日入れたばかりの拒否が、いちばん危ない状態でだけ黙る形)。修正は `lostTargetFold` —— **一度でも宛先を名指ししたセッションでは、省略呼び出しを探索へ落とさない**(生き残りが1つならそこへ戻し、0 か複数なら断る)。名指しの有無は `everNamed*` フラグで持つ(`seenExplicit*` は死んだ機を取り除くので、全滅すると新しいセッションと区別が付かない)。**実装で1回外した**: `survivors.count == 1` を自分で数えていたが、曖昧さの判定は `isAmbiguousMemory` に既にあり、**変異で「その分岐を壊しても何も落ちない」と分かって削った**(規則が2箇所に増えるところだった)。**変異が2件生き残った**のも収穫 —— フラグを立てる行を外しても落ちず、テストが**フラグを手で立てていた**ためだった(台帳が何度も書いている「配線のテストを別に持つ」型)。差し替えドライバの経路は `rememberResolvedTarget` の手前で返るので `call()` 越しには測れず、記録側を直接呼ぶテストにした。最終 4/4 検出 |
| E2EAppCMP・ポートを別の機へ付け替える | ブリッジの建て直しを挟む | 1(2026-08-13) | **タスク**: セッションが port P で機A の木を採った後、`bridge down --port P` → `bridge up --device 機B --port P` で **P を機B のものにし**、同じセッションから古い ref を撃つ。**元からあった欠陥1件**: **キャッシュ命中のドライバの同一性を誰も確かめていなかった**。実測 —— `tap [4] done`(`isError=False`)で**機B の同名要素を実際に叩いた**。注記は「古いスナップショット由来」としか言わず、**機が変わったことに触れない**。前日入れた `keyChangedDevice` は**ドライバ生成時にしか走らない**ので防げない: 建て直しの間セッションは1回も呼ばず `forgetConnection` が走らないため、**キャッシュ命中して生成が起きない**。「死んだブリッジなら失敗経路が拾う」という想定も誤りで、**新しいブリッジは正常に応答する**。修正は「記憶に依存する引数を持つ呼び出し(ref / `steps` の先頭 ref / `bundleId` 省略の `ft_open_url`)に限り、キャッシュ命中時にポートへ直に `/status` を撃って udid を照合し、違えば記憶を捨てて断る」。**実装で1回外した**: 最初は掴んでいるドライバの `status()` を使ったが、実運用のドライバは `SessionRecoveryDriver` に包まれており**建て直し直後のブリッジはセッションが無い**ので 409 で落ち、`try?` が握って**機が変わったときにちょうどガードが黙った** —— 陰性が「常に false を返す検出器」と区別できない形そのもので、**陽性対照を回さなければ気付けなかった**。**修正後の実機確認**: `port 8125 now belongs to a different simulator (was E38DCA93…, now 2C4FBE2E…)` で拒否され、`ft_launch` し直せば通常どおり続行できる。**費用**: 確認は `/status` 1往復(実測6〜9ms)で、ref 系はどのみち木を撮りにブリッジへ行くので限界費用はほぼ無い。全呼び出しに掛けない理由は XCUITest quiescence(実測33.7秒)中に記憶を使わない呼び出しまで詰まらせるため。**却下した案**: 全応答に udid を載せる案(X)は、`BridgeClient` が応答ヘッダを捨てており `AppDriver` プロトコルへの要件追加=全ドライバ改修+両ブリッジ改修+版上げ+フル E2E が要るうえ、**ヘッダは操作の後にしか読めず1回目の誤操作を防げない**。**掃討の抜け(同日中に発見・修正)**: 最初の修正は `direct:ios:<port>:` 経路のキャッシュ命中にしか確認を入れておらず、**`profile:` 経路には穴が残っていた**。しかも profile 経路は `connectedPorts[key]` を記録していないので、確認を足すだけでは**また黙って no-op** になるところだった(材料の記録が先)。両経路で実機の陽性対照を通した(profile 側: `port 8126 now belongs to a different simulator (was …-01, now …-05)`)。再発は `testEveryDriverCacheHitVerifiesDeviceIdentity`(キャッシュ命中の箇所数を2に固定し、各ブロックが確認を呼ぶことを走査)と `testBothPathsRecordWhatTheIdentityGuardNeeds` が落とす |
| E2EAppCMP を iOS 2台 | 2台以上(同一 OS) | 1(2026-08-13) | **タスク**: 2台に同じアプリを入れ、機A を「スクロール画面で `#row_40` を選択」・機B を「セレクタ画面で `#btn_item_3` を選択」まで進め、最後に両方から各自の値を読む。**誤配送が起きると成功したまま同じ値が2回返る**形を狙った。**元からあった欠陥1件**: **機を跨いで ref 番号が衝突する**。`nextRefBase` が engineKey ごとに 0 から始まっていたため、両機の ref が同じ番号空間を使っていた。実測 —— 機A の `[10]` は `#row_30`、機B の `[10]` は `#btn_item_1`。機A の木を見て採った `ft_tap ref: 10` を `port:` だけ機B にして撃つと、**警告も拒否も無く成功して**機B のボタンを叩き、状態が `result=item3` → `result=item1` に変わった。**どちらも button なのでもっともらしく成功する**のが最悪の形で、`refGenerations` の宣言が謳っていた「セッションを通じて ref が一度も衝突しない」という不変条件が2台では破れていた。採番をセッション共通のスカラーへ変更(`nextRefBase`)。**修正後の実機確認**: 機B の ref は 55/57/59 に採番されて衝突せず、機A の `[10]` は `unknown ref [10] — it is not from any recent snapshot` で拒否され、**tap は発射されず状態も変わらない**。回帰は `MCPRefGenerationTests.testARefTakenOnOneDeviceDoesNotHitAnotherDevice` ほか1本(変異2/2検出)。**正しく動いたもの**(この形で初めて確認): A→B→A の交互読みが毎回自分の機の木を返す(監査20 の型は再発しない)/ 省略呼び出しが候補を名指しして拒否される/ 拒否の後も明示指定は通る。**次にこの形をやるとき**: アプリ切替(同一機で launch を跨ぐ)と、ブリッジ建て直しを挟む形はまだ未着手 |
| E2EAppCMP → E2EAppIOS(同一機・兄弟アプリ) | アプリ切替 | 1(2026-08-13) | **タスク**: 同じ機でアプリA を起動して ref を採り、アプリB を起動してからその ref を撃つ。**元からあった欠陥1件**: **ref の世代は機ごとで、アプリでは区切っていない**。E2E の 5 SUT は `#id`・ラベルが共通契約なので、前のアプリで採った ref が次のアプリの木へ再照合されて**黙って当たる**。実測(iOS・同一機)—— `com.ftester.e2e` の `#nav_selector`(ref 54)を採ってから `com.ftester.e2e.ios` を launch して撃つと `tap [54] done` で成功し、注記は「**1px 動いた・周囲のレイアウトが変わった**」と**自信を持って誤説明**した(まったく別のアプリなのに)。`switchedAppNote` は「起動したアプリ = 前面」を見るので明示 launch 後は発火せず、**見ていないのは ref の出自**だった。世代の `sessionBundleID` と今の木のそれを突き合わせて断る(`refFromAnotherAppMessage`)。**どちらかが不明なら何もしない**(旧ブリッジは `sessionBundleID` を返さないので、断ると ref がまったく使えなくなる)。**修正後の実機確認**: `[4] was taken from com.ftester.e2e but the app in front is now com.ftester.e2e.ios — refusing…` で拒否され、素の `ft_snapshot` を撮り直せば新しい ref で続行できる。**正しく動いたもの**(この形で初めて確認): back でアプリを出た後の `⚠️ This tree belongs to …, NOT the app you launched` / `ft_open_url` の `bundleId` 省略時の開示(「前面が変わっているなら bundleId を渡せ」)。**配線のテストを別に持つ**: 純粋関数だけを固定したら「呼び出しを外す」変異が生き残った(レビューが以前指摘した型) |

## 各ラウンドの記録(browser)

台帳①は形ごとの現在地だけを持つ。**個々のラウンドで何を直したかはここ**(長いので分けた)。
出自(自作機構の欠陥か、元からあった欠陥か)の内訳は上の「自作の機構を監査しているとき」の表。

| 記録 | アーキタイプ | ラウンド | 内容 |
|---|---|---|---|
| Safari / Chrome(ブラウザで天気を調べる) | browser | 5(2026-08-13) | 開いている。コーパスに10枚。**5ラウンド目(天気: 今日+週間を両OSで・気象庁 jma.go.jp)**: 実バグ4件。⒜ **ブラウザが webView ノードごと1つも公開しない画面で注記が全滅**(Android Chrome。画面は表で埋まっているのに木は19要素=全部ブラウザ chrome、のち1要素)。`webViewGapNote` は `type == "webView"` の**中**しか測れず、`emptyTreeNote` は `elements.isEmpty` **ちょうど**が条件なので、**状況が悪化したほうが黙る**(直前の読みでは webView ノードがあり警告が出ていた)。画面の 88.6% に要素が1つも無いことを言う `missingPageContentNote` を新設(URL バー在り + webView 無し + 未表現帯 ≥50%)。**URL バーを条件に入れたのは `and-overflow`(56.4%・オーバーレイで背景を落とすのは正常)を除くため** —— コーパス実測は witness 88.6% に対し、URL バー有り webView 無しの次点が `and-browser_urlmenu` の 5.9%。⒝ **`looksUnchanged` が上限適用後の木しか見ず `truncatedCount` を無視**。表を横送りするタップで **113要素増えた**のに、上限120の内側がバイト一致だったため `waitForChange` が「画面は変わっていないかもしれない」と報告した。**呼び手は回避できない**(`maxElements` は `ft_snapshot` にしか無いので、待ちの中の `freshSnapshot` は常に120)。⒞ **横スクロール後、木にスクロール前の複製と実体が同居**(iOS)。同じ y の行が旧 x と新 x(定数 200pt ずれ、左端は x=0 へクランプ)で二重に並び、**約55行のうち印が付いたのは4行だけ**。既存3経路はどれも当たらない —— `outsideDeclaredScroller`/`isUntappableGhost` は「容器と交差ゼロ」が条件で x=0 は容器の**内側**、`stackedRefs` は**同一矩形3個以上**が条件で複製は2個。しかも **WebKit の横スクロール div は `scrollable` を申告しない**(iOS の `scrollableTypes` は scrollView/table/collectionView のみ)ので内側の容器自体が見えない。`duplicateRegionNote` を新設。**判定は「同じ y・違う x で繰り返す最長の連続区間」** —— 素の「最長反復区間」にすると、**このページの2つの表が同じ日付見出し行を共有しているだけ**の形(11行)を掴んでしまい、見出しを共有する普通のページで誤検知する(設計中に実測して気付き、幾何条件を足した)。実測は witness 10 に対し他フィクスチャ最大3。⒟ **木が空同然の画面では「変化なし」判定が常に誤報**(空の木は空の木と一致する)。`scrollDown ×4` が実際に数画面送ったのに `ft_batch` が「どのステップも画面を変えていないかもしれない」と言った。未表現率 ≥50% のとき、判定に「『動かなかった』と『公開されていない』を区別できない」但し書きを付ける(⒜と**同じ `unrepresentedScreenFraction` を共有**する)。**上流で直せないもの**: Chrome が本文を1要素も公開しない / ページ側の横送りボタン「>」も a11y に無く、`ft_swipe scrollFrame` も `ft_drag` も効かないので**座標タップしか手が無い**。**据え置き1件**: `ft_scroll_to` の `maxElements`(理由は監査24と同じ)。ただし**2ラウンド連続で同じ壁**に当たったので、代案「探索中に上限へ当たったら自動で天井まで上げて再試行」を候補として記録する。**性能の観測**(修正せず): `ft_scroll_to` が1スワイプ約20秒(78.1s/6スワイプ・65.6s/3スワイプ)で、失敗すると `reverseSweep` により**開始位置より手前のページ先頭まで戻る**が、応答はそれを言わない |
| ↑ 4ラウンド目までの記録 | browser | 4(2026-08-13) | コーパスに8枚。**4ラウンド目(天気: 今日+週間を両OSで・Yahoo!天気)**: 実バグ4件。⒜ **`gridWithoutHeaderNote` が実アプリで初めて誤検知**(iOS Safari で同じページの2つの表に発火)。**見出し行が値の列と centerX で揃っていると、見出しが鎖の最上行として取り込まれ**、その上の段落間が「見出しが無い」と読まれる。「直上が空か」だけでは見出しの在る格子と区別が付かない —— **見出し行が入る余地**(直上の空き ÷ 行間の中央値 ≥ 2.0)を要求して解消(実測比: 誤検知 1.16 / 0.54 に対し真陽性 4.4)。⒝ **`webViewGapNote` が閾値超えの帯を1本しか言わない**(`largestEmptyBand` が最大の1本を返していた)。Android の週間画面では 345px の帯だけが報告され、**黙って落ちた 268px のほうに週間表の日付・気温・アイコンが丸ごと入っていた** —— 読み手は「警告された1箇所以外は揃っている」と読む。閾値超えは全部数え、名指しは3本まで・残りは件数で言う。⒞ **値の行が1本だけ生き残った形はどの注記も出ない**(`gridMinRows=2` なので格子と見なされない)。**最も誤読しやすい形が最も無警告**だった —— ⒝ の修正で、その行の直上の空白として報告されるようになった(新しい検知は足していない)。⒟ **`ft_scroll_to` が読み取り整合性の注記を落としていた**(`urlishLabelsNote` / `ambiguousLabelsNote` / `duplicateIDsNote` が `.snapshot` 限定)。`link "13101"`(実際の描画は「千代田区」)と `"40％" ×3` を無警告で返し、**同じ画面を `ft_snapshot` で撮り直して初めて出た** —— ft_scroll_to は自ら勧める常用経路なので、警告の落ちる側が既定になっていた。改善は `emptyTreeNote`(要素0の木を「遷移中かもしれない」と言う。Chrome の初回起動ダイアログを閉じた直後に `screen:` の1行だけが返った)。**据え置いた1件**: `maxElements` を `ft_snapshot` にしか置かないのは意図的な設計(操作系・scroll 系は木を何度も撮るので、1回限りの指定がどの取得に効いたか説明できない)。**上流で直せないもの**: Android Chrome は週間表の日付・気温・アイコンを a11y に出さない / `link "13101"` は `urlishLabelsNote` が数字のみを意図的に除外している(本文の数値と区別が付かないため) |
| ↑ 3ラウンド目までの記録 | browser | 3(2026-08-12) | コーパスに6枚。**3ラウンド目(天気: 今日+週間を両OSで・tenki.jp)**: 実バグ4件。⒜ **格子の見出し行が木から落ちても無警告**(Android Chrome の2週間表。値のセルは 4列×3行そろっているのに「日付 / 12日(水) / …」が1つも無く、**列と日付を対応付けられないまま読める**)。原因は2つで、`webViewGapNote` が帯を**60分割スライスの本数**で測っていたため実効閾値が宣言上の8%でなく最大11.3%になっていた(実測: 実際の帯 192px=8.9% が 143.5px=6.7% と数えられて不発)ことと、そもそも「格子である」ことを言う注記が無かったこと。帯は**実要素のエッジまで伸ばして実測**に直し、`gridWithoutHeaderNote` を新設(隣接行の centerX 一致で列を鎖状に繋ぐ = 全数で誤検知0)。⒝ **`ft_batch` が空振りを成功と表示していた**(`swipeBy` が `ok` を返し木は操作前とバイト一致。同じジェスチャを `ft_swipe` で撃つと警告が出る)—— 最後の木が settle-lite を通っていなかった。⒞ **`StepExecutor` の `scroll` アクションが `scrollFrameRect` を見ておらず**、ref 形の `scrollFrame` が黙って全画面スワイプへ退化していた(`flick`/`scrollTo` は見ている)。⒟ **`ScenarioCodeGen` に `case "scroll"` が無く**、`ft_batch` で実行できる `scrollDown/Up/Left/Right` が書き戻せなかった = 「通ったバッチは 1:1 でシナリオ行になる」契約の破れ。改善は `ft_swipe` の `scrollFrame`(実装は `StepExecutor` へ委譲)・`ft_open_url` の `snapshotAfter`・`addressBarNote`(同じ URL でも Android は `/lite/` へリダイレクトされ**別ページを読んでいた**)。**撤回した最適化1件**: 0スワイプの `ft_scroll_to` で撮り直しを省く案は、既存の回帰テスト2本が落ちて**撮り直しが節約対象ではなく砦**だと判明したので戻した(docs/design.md の当該項)。**残り(未実装)**: 横スクロール後の iOS は**表のセル全部が x=0 を報告**し、`⚠️scroll-leftover` が37行まとめて立つ(判定は与えられた木に対しては正しく、WebKit が a11y frame を更新していない上流の問題。`compose-ios-ax-frame-clamp` と同型) |
| ↑ 2ラウンド目までの記録 | browser | 2(2026-08-12) | **2ラウンド目(天気: 今日+週間を両OSで)**: 実バグ3件を修正済み —— ⒜ **要素上限が web ページの本文だけを削っていた**(tenki.jp の2週間表で 128 件脱落・全部 labelled。落ちた行を `ft_scroll_to` が探して2回 101 秒を空費)。`?max=` / `ft_snapshot maxElements:` で呼び手が1回だけ引き上げられるようにし(版 65・天井 400)、打ち切り注記が実際の推奨値を名乗る。実測 120→2行 / 400→248 要素・14〜18日の行が入る ⒝ **探索の途中で当たった打ち切りを最終木からは知りようがなく**、失敗文が「見つからない」としか言わなかった(`StepNote.truncatedDuringSearch`) ⒞ **Android の Chrome が web コンテンツを部分的にしか公開しない**のに無警告だった(画面に描かれている表・本文がフルツリーにも無い)。幾何で穴を検知して警告する `webViewGapNote`(閾値はコーパス実測: 取りこぼし有 11.7% / 無 0〜3.3%)+ URL 断片がラベルになったリンクを名指しする `urlishLabelsNote`。XCUITest ランナーの日本語エラー 13 本も英語化(`ft_type` の 422 は原因を名指しするようにした)。**残り**: 旧ラウンドの**バグ3件は修正済み**(改行入りラベルが1行1要素の契約を壊し勧めたセレクタも書けない / `ft_type` の 500 が「既に撃った tap をもう一度やれ」と助言していた / `SweepHarnessTests` の採り直し出力が 30 枚で4枚ぶん落ちていた)。言い回し4件も実装済み。**残り**: ⑴ Android の WebView は img alt を落とし、リンクのラベルが内部コードになる = **上流(Chrome の a11y)の制約で ftester からは直せない**(`and-browser_weather` が witness) ⑵ **オーバーレイ配下をまとめて1行で言う要約は未実装** —— 幾何判定は正しく1件ずつ出ている(`ios-browser_startpage` は overlay 64 + keyboard 55 が全部真陽性)が、全画面オーバーレイの判定は新しい検知になるので `Scripts/mcp-bench.sh` で手数が動くか測ってから ⑶ `⚠️scroll-leftover` の誤検知を実機で1度観測したが**再現せず**(その瞬間の木では下端に貼り付く広告が実際に3行を覆っており、判定は与えられた木に対しては正しい。木が実描画から遅れていた疑い) |


## 記録の置き場

- **フィクスチャ**: `Tests/Fixtures/RealAppSnapshots/`(README にアーキタイプの由来表)
- **検知の回帰**: `SweepHarnessTests`(幾何判定の件数)/ `NoteCoverageTests`(注記の発火とバイト)
- **注記の予算**: `NoteBudgetTests`(本数と鍵の集合を**等号で固定**。増減は台帳へ根拠を書いてから)
- **手数**: `Bench/`(`Scripts/mcp-bench.sh`)。**回した結論は `Bench/measurements.md`**
  (生の記録は `.ftester/` = gitignore なので残らない)。**実 web ページの形は測れない** ——
  そこがいま注記を最も産んでいるので、当面は上の予算で代用している(Bench/README.md 冒頭)。
  2026-08-13 に **Android の settings / chat** を足した(それまで全タスクが iOS で、
  未解決の witness が Android にしか無い状態で設計判断を保留していた)
- **台帳**: このファイル
