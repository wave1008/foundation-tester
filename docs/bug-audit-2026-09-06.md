# バグ出し台帳(2026-09-06)

対象: HEAD 77165770。手法: `swift test --parallel`(5,082 件緑)・`npm test`(1,335 件緑)・
8 プロジェクトの dry-run(project1 のみアサーション無しで意図どおり赤)・MCP の JSON-RPC 枠を
stdin で確認したうえで、12 領域を並列コードレビューし、上がった指摘を実コードで裏取りした。
**「再現」= 実行して確認 / 「確認」= コードパスを読み切って確認 / 「未検証」= レビューの指摘のみ**。
未検証の項目は行番号と失敗シナリオが具体的なので、直す前に1件ずつ読めば足りる。

環境メモ: この Mac の FM は現在 text/vision とも死亡と報告される
(`com.apple.SensitiveContentAnalysisML error 15`)。ツールの判定は正しく動いている。


## 0. 修正状況(2026-09-06 同日・未コミット)

**直したもの**(バッチ1〜3。`swift test --parallel` / `npm test` 緑。ローカル台だけの E2E は
E2E-CMP(iOS in-app・Android)/ E2E-Flutter(iOS in-app)/ E2E-Android で確認):
- R1〜R4 の再現4件(チャンク分割 / MCP `port` / batch の整数 / `String?` nil)
- `remote clean` の find 深さとグロブ(`RemoteCleanPlan.commands` は2本の find へ)
- 注記無しプロファイルの `--machine` 送り(`RunnerProfileView` が `.wholeProfile` を全台 local へ)
- 不正な正規表現の事前検証(`RegexValidation`。セレクタ `…Matches=` / DSL `…Matches(Not)` / `thisMatches(Not)`)
- `#x[2]` の序数 / `…Matches=` 内の `(a|b)` 非展開 / `\r\n` の畳み / `!` 接頭辞のエスケープ
- 焦点救済経路の読み返し(`verifyTypedText`)
- in-app ブリッジ: メイン待ちの timeout を 504 に(天井は `BridgeAPI.inAppMainThreadWaitMs` = ホスト 20s から逆算した 15s。
  当初 4s で E2E-CMP が1本赤になった)/ `type("…\n")` の Enter 不発を 422 に(版 90)
- FM プローブの死判定を経路ごと連続 `FMBreaker.threshold` 回に
- 空クラスの一括実行 / machineProfilesTab の ReferenceError / lastResultsSync の project 追従 / stepsView の鍵 /
  `scenarioRequeued` の実行中バッジ(拡張 0.0.797・install-local 済み)
- Android: 整形する欄の `type`(before からの変化で反映と見る)/ 録画が自分の screenrecord だけを止める(版 66・APK 再ビルド済み)


**フル E2E(`Scripts/e2e.sh --align`、1675525f・3機分散)**: 8 プロファイル中 6 緑。赤 2 件はどちらも今日の変更由来ではない:
- E2E-iOS / ios-inapp(M1Ultra): 「ジェスチャが正しく検出されること.S0010」が 180 秒で打ち切り。**9/1 以降 M1Ultra では常に 150〜240 秒**
  (`select` が 1 回 4〜5 秒 = FM 生存機の occlusion-guard コスト。9/3 にも 4 回打ち切り)。FM の死んだ手元では 35 秒。
  → 台帳 §3 の「スクショ/FM 待ちで周期が伸びる」と同根。対処は scenarioTimeout の見直しか、FM の待ちを select に載せない設計
- E2E-RN / ios-inapp(local): 「待機とタイムアウト」S0020(フル)/ S0010(負荷付き再実行 3 周中 1 周)。単発 3 回は緑。
  形は両方同じ: **同じ台 -05** で `tap`(activate 不発 → 合成タッチ)が 7 秒(通常 0.55 秒)かかり、1/3 秒後に出るはずの
  `#txt_delayed` が出ない = **RN の合成タッチが負荷下で落ちる**(InAppBridge.tapByRef の synthFallback は成否を返せない、
  と自らコメントしている既知の穴)。履歴 153 回中 3 回。tap の経路は今日触っていないが HEAD 対照は未実施。
  次に出たら `FT_BRIDGE_TIMING` 相当のプローブを 1 周目から入れる

- バッチ4(同日): hybrid の `press(ref:)` を `active` へ / `home()`・`appSwitcher()` 後の snapshot は再前面化せず SpringBoard を読む
  (`HybridFallbackDriver.backgroundSnapshot`。MCP 実機で `ft_navigate home → ft_snapshot` がホーム画面の木を返しアプリは背面のままを確認)/
  `press`・`drag`・`pinch` の HTTP 上限に duration を足す(`BridgeClient.timeout(forDuration:)`)/ 振り直し上限で失敗記録を残す
  (`RunOrchestrator.requeueDiscardingRecord`。合成 skipped 記録は書かない)/ fan-out の子は諦めた後も 60 秒ごとに rsync からやり直す
  (`RemoteMonitorFanout.slowRetrySeconds`)

**直していないもの**: §2 のうち上に無いもの(実機の自動起動・
`irregularHandler` の `||`・入れ子 scene・dry-run の `ifCanSelect`・
`FMUsageLedger` の自 pid・bridge watchdog の `inRun`・`BridgeLauncher.stop()` の ps 照合・iproxy の UDID・
`XCUIBridgeResolver` の PortHolder・update.sh のスキル複製・リモート機の GPU 再起動・offline エミュレータの wipe・
`textIs` の SpringBoard 経路・見切れ回復の帳簿・`ft_list_apps profile:` の platform・logcat の TZ)と §3 の全部。

## 1. 再現したクラッシュ・誤判定(4件)

| # | 場所 | 内容 | 再現手順 |
|---|---|---|---|
| R1 | `Sources/FTCore/RunResultsStore.swift:369-383` | 並列走査のチャンク分割で `start > end` の Range を作り **`fleetest api results` がプロセスごと落ちる**(exit 133)。条件は記録数 n が `(cores-1)*ceil(n/cores) > n` を満たすとき(24 コアなら n=25〜45 など)。受け手の新しいプロジェクトが最初に踏む。拡張のダッシュボードも同じ経路 | `results/runs/<月>/<run>/scenarios/` に 30 件置き `api results --no-cache` |
| R2 | `Sources/fleetest-mcp/MCPServer+Driver.swift:314`(同型 `+Dispatch.swift:468`, `+ScenarioTools.swift:136`) | `port` に 65535 超・負数を渡すと `UInt16.init` がトラップし **MCP サーバが落ちる**(セッションの ft_* が全滅) | `ft_status` に `{"port":70000}` |
| R3 | `Sources/fleetest-mcp/BatchLineParser.swift:478` | `repeat`/`maxSwipes`/`ref` の大きい値・指数表記で `Int(Double)` がトラップし MCP サーバが落ちる | `ft_batch steps:"scrollDown repeat: 99999999999999999999"` |
| R4 | `Sources/FTDSL/ValueAssertions.swift:204` | `String?` の nil に `thisIsNotEmpty()`/`thisContains()` を呼ぶと `"nil"` という文字列として判定され**緑になる**(Optional がジェネリック経由で `Optional<Any>.some(.none)` に包まれる) | `let s: String? = nil; s.thisIsNotEmpty()` → passed |

## 2. コードで確認した不具合(重い順・領域別)

### 誤った緑(検証が成立していない)

- **不正な正規表現が「不一致」扱い** — `StepExecutor+Resolve.swift:281`, `ValueAssertions.swift:136`, `Flow.swift:207`。`range(of:.regularExpression)` は不正パターンで nil を返すだけなので、`textMatchesNot("エラー(")` や `notExist("textMatches=(x")` が**永久に緑**。事前検証(`NSRegularExpression(pattern:)`)がどこにも無い
- **`#x[2]` が id リテラル `"x[2]"` になる** — `FTSelector.swift:441`。docs/commands.md:548 は序数と書く。`notExist("#row[2]")` が緑。`validationError` も通す
- **`textMatches=^(?:foo|bar)$` の `(a|b)` がフィルタ内 OR として展開される** — `FTSelector.swift:353`。`^?:foo$`(不正)と `^bar$` に割れ "foo" が見つからない。量指定子付き `(a|b){2}` も壊れる
- **焦点救済経路が読み返さない** — `StepExecutor+Actions.swift:361-372`。`tap(欄)→type(文字)` の救済で `driver.type(ref:)` を撃った後 `verifyTypedText` を呼ばない(in-app は `verifiesTypedText=false`)。in-app では `focused` が `isFirstResponder` 由来なので Compose/Flutter の欄は常に nil → 毎回この経路に入る(注記も毎回出る)
- **`String?` の nil**(R4)/ **判定不能を `assertion` に丸める** — `StepExecutor.swift:557`。`undecidableTruncationMessage` と「keyboard state 不明」の `.failed` が failureKind=assertion になる(CLAUDE.md「言えないときは欄ごと省く」に反する)
- **in-app が「未実行の 200」を返す** — `InAppBridge/Sources/InAppBridge.swift:1154`(`performWithSettle`)と `:524`。`sem.wait(timeout:)` の結果を捨てるので main が塞がっていると操作は後で実行されるのに 200。遅れて立つ `lastSettleCapped` が無関係な応答に付く
- **`type("…\n")` の Enter 失敗が隠れる** — `InAppBridge.swift:591` `inserted || enterFired`。本文が入れば Enter が NO でも 200(Flutter の returnKey 既定で発生)
- **`screenLooksLike` が skip でもアサーション計上** — `FTRuntime.swift:636`(未検証だが読み筋は単純)

### 実行が壊れる・止まる

- **`remote clean` が results を丸ごと消す** — `Sources/FTRemote/RemoteDispatch.swift:943`。`find <…>/results -mindepth 1 -maxdepth 1 -mtime +N -exec rm -rf` は `results/runs` ディレクトリ自身を判定する。実体は `results/runs/<YYYY-MM>/<run>` で `runs/` の mtime は月ディレクトリ作成時にしか動かない(手元の E2E-iOS の `runs/` は Sep 1)。**月初 + keepDays 日を過ぎると当月ぶんを含む全 run 記録・on-demand の録画が消える**。同じ配列は ssh 越しにグロブも書いている(zsh の nomatch で 3 本が毎回 exit 1)
- **注記の無いプロファイルを `--machine` で送るとデバイスが全部消える** — `Sources/FTCore/RunnerProfileView.swift:47-58`。`RemoteDispatchDeviceScope` は「全台 machine 未指定 = 丸ごと送る」と定義するが、`localizeMachineProfile` は `effectiveMachine == alias` でない台(nil)を全部落として上書きアップロードする。`profile setup` が作る素のプロファイルがこの形
- **align/setup 中の Ctrl-C で dispatch.lock が残る** — `Sources/fleetest/RemoteSetupCommand.swift:33`(`runInheritedSSH` に `InterruptRelay` 無し)。以後の `run --machine` が「another dispatch is running」で止まる
- **空クラスで GUI の一括実行が全滅** — `vscode-fleetest/src/testTree.ts:249` が子無し class ノードを作り、`runHandler.ts:293` が「子 0 = leaf」と読んで `--scenario class:/X` を渡す → `RunScenarios.resolve` が throw し run 全体が errored。最後の @Test を削除した直後に起きる
- **マシンプロファイルのマシン切替が ReferenceError** — `vscode-fleetest/src/webview/monitor/machineProfilesTab.js:145,171` が未定義の `validateSelectedDeviceName()` を呼ぶ(定義は `validateSelectedDeviceKeys`)。一覧とフォームが前のマシンのまま
- **Android の整形する入力欄で `type` が必ず 500** — `AndroidRunner/src/com/example/ftbridge/InputInjector.java:222`。適用確認が完全一致のみなので、`PhoneNumberFormattingTextWatcher`・`AllCaps`・`maxLength` の欄は「入っているのに `value did not change`」で 4 秒後に赤。途中で BACK 注入も走る
- **録画が配信を殺す** — `Sources/FTCore/AndroidScreenVideoRecorder.swift:59,154` の `kill -2 $(pidof screenrecord)` は端末上の全 screenrecord を殺す。モニターの `fleetest-androidstream` も screenrecord なので `record: true` の run の開始・終了ごとに配信が落ちて再起動される(通常運用でモニターは止めない)
- **実機で XCUITest ブリッジの自動起動が成立しない** — `Sources/fleetest/LiveBridgeAutoStarter.swift:162` / `Sources/FTBridgeClient/XCUIBridgeResolver.swift:150`。`IOSDeviceTransport.establish` を通るのは `BridgeProvisioner.swift:906` だけで、この 2 経路は loopback で `waitUntilReady` → 180 秒後に失敗しランナーを止める
- **`press`/`drag`/`pinch` の壁時計上限が固定 20 秒** — `BridgeClient.swift:48,843`。`tap(sel, holdSeconds: 25)` は DSL に上限が無いので `bridgeUnreachable` になり、ランナーはまだ押している
- **hybrid の `press(ref:)` が別名前空間の ref を撃つ** — `HybridFallbackDriver.swift:158`。`delegatedApp`(springboard 等を launch 中)を見ず、in-app の木の同番号の要素を長押しする
- **hybrid で `home()` 後の snapshot がアプリを前面に戻す** — `AppAttachDriver.swift:60`(snapshot ごとに `activate`)。MCP の注記は「木は最後の状態」と逆のことを言う

### 事実の消失・集計の誤り

- **振り直し上限到達で失敗の一次記録が消える** — `RunOrchestrator.swift:936-957`。`discardLast` を `requeue` の可否より先に撃つので、2 回目の凍結判定では `failedSteps`/`errorLogs`/`timeline` を持つ記録が消え、`skipped` の合成記録だけ残る
- **`irregularHandler` が `||` の代替節を捨てる** — `Sources/FTDSL/Commands.swift:84-95`(`.primary` だけ渡す)。日本語版のモーダルなど第 2 節は永久に閉じない
- **入れ子 scene 後のステップが内側 scene に記録される** — `FTRuntime.swift:1280`(末尾固定)
- **dry-run で `ifCanSelect` が常に true** — `FTRuntime.swift:1138`。`.ifElse {}` の中身(構文誤り・台帳に無い `#id`)がデバイス実行まで出ない
- **FM 死活プローブが単発失敗で「死」を書く** — `FMLivenessProbe.swift:152`。`FMHealth` は経路ごと `FMBreaker.threshold` 回で判定するのにプローブは 1 回。一過性エラー 1 回で ≥60 秒 dead になり run.json の `fmDead` にも載る
- **`FMUsageLedger` が自 pid の古い控えを掃除できない** — `FMUsageLedger.swift:190`(生存 pid は残す)。pid 再利用時に旧累計ぶんの呼び出しが全読み手で落ちる
- **`\r\n` が表示と照合で食い違う** — `TextNormalization.swift:72`(1 クラスタ 2 スカラなので空白に畳まれない)。木には `"a b"` と印字されるが `exact` 照合は不一致
- **`serialize` が `!` 接頭辞を逃がさない** — `FTSelector.swift:817`。`"!重要"` を往復すると否定フィルタに化ける。`||` は `=` エスケープしても分割が先(:70)
- **`scenarioRequeued` でタイルの「実行中」が消えない** — `vscode-fleetest/src/runLaneModel.ts:309`
- **プロジェクト切替後に CLI 実行の結果が Test Explorer に反映されない** — `lastResultsSync.ts:181`(activate 時のプロジェクトだけ `fs.watch`)。**ステップ一覧のキャッシュもプロジェクトを跨いで混ざる** — `stepsView.ts:233`(鍵が scenario id だけ。5 SUT は同名クラスが多数)
- **`ft_list_apps profile:<android>` が iOS 分岐へ落ちる** — `MCPServer+Dispatch.swift:104-116`(`platformName` が `profile` を見ない。同型 4 箇所)
- **logcat の `-t` 時刻をホスト TZ で整形** — `AndroidLogcat.swift:86`(端末は端末ローカル時刻で解釈)

### 運用・監視

- **fan-out の子は一度諦めると monitor 再起動まで張り直さない** — `RemoteMonitorFanout.swift:107,129`。ランナーがスリープ中に VSCode を開くと、復帰後もその機械は `unknown` のまま・配信も畳まれたまま
- **bridge watchdog が `inRun` を見ない** — `monitorBridgeWatchdog.ts:80-101`(health watchdog は見る)。CLI からの run 中の台に `device-up` を撃つ
- **`BridgeLauncher.stop()` が pid を ps 照合せず kill** — `BridgeLauncher.swift:346`(`reapRunnerProcess` は照合する)。TTL 自主終了で `.pid` が残った後の pid 再利用で無関係なプロセスに SIGTERM→SIGKILL
- **iproxy の再利用が UDID を照合しない** — `IOSDeviceTransport.swift:270`。別の実機へのトンネルを掴む
- **`XCUIBridgeResolver.freePort` が `PortHolder` を通らない** — `XCUIBridgeResolver.swift:166`(CLAUDE.md「起動前に LISTEN 実体を確かめる」)
- **プラグイン利用者にもスキルのコピーが生える** — `Scripts/update.sh:207-236`。`fleetest init` が `.claude/skills/fleetest-setup` を必ず作るので `[ -d ]` 判定が常に真 → 5 本コピー → `fleetest-update` と `fleetest:fleetest-update` が両方並ぶ
- **リモート機の「GPU で再起動」が手元の同名機を再起動** — `deviceTiles.js:945`(name だけ送る)→ `monitorDeviceOps.ts:297`(手元で実行)
- **offline のエミュレータを「未起動」と見て稼働中の下からイメージを消す** — `AndroidDataWiper.swift:181`(`runningAVDs` は `state=device` のみ。`allEmulatorSerials` があるのに使っていない)
- **`textIs` の SpringBoard 経路が門に取り消される** — `StepExecutor+Assert.swift:659`(`resolvedViaSystemUIThisStep` を立てるのは `exist` だけ)。`iosAlertHandler` 登録中に `textIs("許可",…)` を書くと門がアラートを閉じて再判定し赤
- **見切れ回復の `continue` が帳簿を素通り** — `StepExecutor+ScrollSearch.swift:444-475`。`swipes`・`previousSnapshot`・`scrollFrameUnresolved` の fail-fast を通らないので、解決しない `scrollFrame` でも全画面スワイプが撃たれ、`swipes` は 0 で返る

## 3. レビューの指摘で私が未検証のもの(行番号つき。読めば判定できる)

- StepExecutor: `+Settle.swift:695,715` gestureFallbackLatched が path 付き 501 で path 無し swipe まで XCUITest 化 / `+Assert.swift:148` `firstFrameGatePending` が FM が答えた回にしか消費されない / `+Actions.swift:881` fb で解決した要素を primary の木で取り直す(ref 名前空間の混在)
- Selector: `LocatorFingerprint.swift:35` 指紋解決だけ `hasClampedCoordinates` を除外しない / `SelectorNaming.swift:102` 末尾 `*` を stable と勧める・括弧不均衡ラベルに書ける形が無い / `FTSelector.swift:441` `#`(空 id)が検証を通る / `serializeNot` が多属性を潰す / `LocatorFingerprintCache.swift:59` 通らなかった分岐の指紋も刈る
- Run control: `JUnitReportWriter.swift:55` `noWorker` が `<skipped>`(failures=0) / `FMLock.swift:68` 設定再読込で保持中の flock を全解放 / `FMLiveness.swift:214` 無ロック RMW / JUnit の制御文字 / `RunOrchestrator.swift:954` 合成記録の worker 綴り / `ScenarioHost.swift:292` ランナー不在で記録なし / `RunResultsStore.swift:214` startedAt 不正の run が窓に混ざる / `ApiRunCommand.swift:314` Android 1 本で iOS も 2 台 / `HostMetricsSampler.swift:81` rotate 失敗で閉じた fd
- BridgeClient: `BridgeProvisioner.swift:756` adopt が LAN 宛先を無視 / `HybridFallbackDriver.swift:106` home 後の rotate 復元 / `IOSSimulatorVideoRecorder.swift:190` 再 spawn 中の stop で孤児 .mov / `:272` reachedEdge が primary の古い値
- Android: `BridgeRouter.java:449` 前面判定で許可ダイアログの pkg を force-stop / `QuietWaiter.java:67` IME の WINDOW_STATE_CHANGED で静穏対象が IME へ / `BridgeRouter.java:301` snapshot 前の swipe が 1080x2400 仮定 / Trichrome / `@avd` 形 / `AdbInstallVerifier` 2 プロセス
- CLI/リモート: `RemoteRunDispatcher.swift:321,360,652` relay の無い窓 / `DeviceMachineRunner.swift:57` `--failed`/`--report-dir` を捨て・ローカル子に `--skip-build` 無し / `RunnerProfileTransfer.swift:61` `waitUntilExit` を協調スレッドで / `forwardControl` SIGPIPE / `print` バッファ / `transferWebViewCache` の ssh に BatchMode 無し / `acquireDispatchLock` が 255 以外を held と読む
- 監視: `Shell.run` timeout 無しの毎周期コマンド(simctl list / getprop / screencap)で監視ループが永久停止 / `ApiMonitorCommand.swift:1016` debounce で `wired` 落ち / スクショ直列で周期が台数×timeout
- MCP: `+Dispatch.swift:776` `ft_type replace` の世代ずれ / `+Snapshot.swift:1105` `scrollFrame` の型検査漏れ / `ft_press` が記憶適用外 / `ft_status android` 複数台 / batch の `\` 落ち / draft の className / `-32700` 無応答
- 拡張: `cli.ts:120` cancelCurrent の誤爆 / 準備フェーズ中のキャンセル無視 / `debugAdapter.ts:452` 時限 SIGKILL / `cli.ts:211` stdin error 無し / `monitorProcessManager.ts:485` 自己再起動を失敗に数える
- webview/スクリプト: `liveTab.js:382` タップごとに h264 デコーダ破棄→stall / `deviceTiles.js:1087` 遅延ポーリング 1 枚で破棄 / `.mcp.json` の `bash -lc` / `install.sh:227` tee 二重 / `align.sh:105` grep 0 件で死ぬ / `install.sh:316` `| head` SIGPIPE / `settingsTab.js:138` fmConcurrency 既定値の固定化 / `preflight.sh --work-dir` 無視 / curl 形 install.sh の TOOL_ROOT
- iOS ブリッジ: `BridgeRouter.swift:1405` 座標ジェスチャが `.runningBackground` を素通し(背面で Find the Application → ランナー死) / handler 文脈の要素スコープ XCUI 呼び出し(TOCTOU) / `activateSnapshotNode` が古い木の要素を発火 / `frontmostTouchableWindow` と木の窓順序 / 型語彙の割れ(SearchField/PickerWheel/ScrollView) / build.sh arm64 固定 / `InAppSettle` が keyWindow のみ

## 4. 直す順の提案

1. R1〜R3(クラッシュ。修正は各 1 行: 空チャンクの guard / `UInt16(exactly:)` / `Int(exactly:)`)
2. `remote clean` の find 深さ(データ消失)/ RunnerProfileView の注記無し台
3. 誤った緑の群(正規表現の事前検証・`#x[2]`・`(a|b)` 展開・Optional nil・焦点救済の読み返し・in-app の未実行 200)
4. 空クラスの一括実行・machineProfilesTab の ReferenceError・InputInjector の整形欄(受け手が日常で踏む)
5. 残り
