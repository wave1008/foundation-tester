# バグ出し台帳(2026-09-11・実走 04:50〜07:10 JST)

対象: HEAD 58f3e77f(作業ツリーはクリーン・リモート2機も同じコミットで適合)。
手法: **ツールを実際に走らせて**拾う(コードレビューではない)。

- フル E2E(`Scripts/e2e.sh --align`)を2周。2周目は `--record`(録画整合チェック付き)
- フリート外の物理端末(Pixel 4a / Pixel 3a)で E2E の実機プロファイルと MCP を直接操作
- 時刻付きの単発ディスパッチ・中断(SIGINT)・一時シナリオでの狙った再現
- 並行して4本の探索を委譲(CLI の境界値 / MCP × 物理 Android / MCP × iOS XCUITest / MCP × iOS in-app)。
  **委譲の報告は自分で再現またはコードで裏を取ったものだけを本文に上げ**、残りは §14 に「担当の報告」として分けた

**「再現」= 実行して確認 / 「確認」= コードパスを読み切って確認 / 「未確認」= 担当の報告のみ**。
`swift test --parallel` は開始時に緑(5,792 件)。`npm test` も緑(1,583 件中 1,581 合格・2 スキップ)。

**検証できなかった範囲**: 手元 Mac の FM が全時間帯で死亡(§15)のため、**自己修復・トリアージ・occlusion-guard・screenLooksLike は
手元では1度も効いていない**(M1Max/M1Ultra では効いていたが、05:16 以降は §7 のロックでリモートに出せなかった)。
リモートを含む E2E は r1 の1周だけ。**物理 iPhone**(XCUITest しか使えない経路 = F30/I2/I4/F31 が効く先)は、iPhone 13 のブリッジ起動が
端末側の UI 自動化の有効化待ち(`Timed out while enabling automation mode`。人手でロック解除・承認が要る)で止まり、当てられなかった。

**結果 DB の汚れ**: 2026-09-10 19:50Z(09-11 04:50 JST)以降の run は全てこのバグ出しのもの —— 意図的な打ち切り(`--set scenarioTimeout=3`)・
同じ台への同時実行・一時シナリオ(`F9凍結誤判定` / `未使用コマンドの当て`)・中断を含む。flaky / insights を読むときはこの時間帯を外して読む
(`results` 系は `--since` しか取らず、直近の時間帯だけを外す口が無い = F27 の「上書きが記録に残らない」と合わせて後から機械的には分けられない)。一時シナリオのファイル自体は削除済みで、シナリオ実行バイナリも建て直し済み。

---

## 0. 要点(重大度順)

| # | 重大度 | 内容 | 確度 |
|---|---|---|---|
| F13 | 大 | Android: 別の欄に焦点がある状態で `tap(容器)` → `type` が**前の欄へ入って緑**(Shirates 伝統形の誤った緑) | 再現(物理 Pixel 3a・エミュレータ) |
| F9 | 大 | iOS in-app: システムアラートに覆われた台を**「画面凍結」と誤判定**して離脱・ブリッジ停止・同じ台で再実行 | 再現(M1Max で2回・手元で1回) |
| F28 | 大 | 拡張の「テストを中断」(= `api run` へ SIGTERM)で `api run`(CLI の `run` も)が**即死**し、録画の `simctl recordVideo` が**孤児として録り続ける**・index.json 無し | 再現 + 両側のコード確認 |
| F30 | 大 | XCUITest エンジン: 焦点が無い `type` が **ok(何も入らない誤った緑)+ 直後にランナー死亡**。in-app は正しく 409 | 再現(DSL・MCP の両方) |
| I2 | 大 | XCUITest エンジン: パスワード欄を打ち直すと**伏せ字「•」が文字として入り、DSL は ok**(`abc`→`XY` で `XY•XY`) | 再現 |
| I4 | 大 | XCUITest エンジン: 権限アラート中にアプリの要素を ref で叩くと **XCTest が黙って「許可しない」を押す**(注記は「何も届かない」と言う) | 再現 1/1 |
| N1 | 大 | **in-app エンジン(既定)**: 座標タップが画面外・容器の外・キーボードの下の**見えない要素を押す**((201,900) で画面外の `#nav_diagnostics` へ遷移) | 再現 |
| F10 | 大(性能) | リモート回収後の走査が手元の結果 DB 全件を2回読む = **ディスパッチごとに約50秒**、手元レーンが遊ぶ | 再現(時刻付き)+ 単独計測 |
| F12 | 大 | 複数機械 run を Ctrl-C 相当で中断 → **リモートの dispatch.lock が残り以後の全ディスパッチを拒否**。原因の SIGPIPE 即死は手元でも再現(`| head` で rc=141・録画の孤児7本) | 再現 |
| N3 | 中〜大 | in-app: 入力欄でないセレクタ・ref への `type` が**焦点のある別の欄へ入って ok**(`type "#btn_input_submit" "Q"` → `single=Q`) | 再現(DSL) |
| F29 | 中〜大 | 手元の同じ台へ2つの run を同時に当てても**無警告**で互いを壊し、赤は全部「テストの失敗」として記録 | 再現 |
| N5 | 中 | in-app: キーボードを閉じても `keyboardShown: true` のまま → DSL の `keyboardNotShown()` が誤った赤・`keyboardShown()` が誤った緑 | 再現 |
| F11 | 中 | 未登録の権限アラートが run を跨いで残り、**後続の全 SUT を汚染**(要求元を終了しても消えない) | 再現 |
| F7 | 中 | iOS の Wipe Data(`simctl erase`)が**ロケールを捨てる**(Android は戻す) | 再現(使い捨て sim)+ 確認 |
| F5 | 中 | リモート実行の reportPath(画面・`api run`・JUnit)が**手元に存在しないパス**を指す | 再現 |
| F2 | 中 | 全レーン復活不能のとき、最後の発話が原因(FATAL)ではなく「`devices up` で起動しろ」 | 再現 + 確認 |
| F25 | 中 | 非 debuggable の物理 Android(13)で WebView の DOM が読めないとき、**設計上出すはずの警告が出ない**(dumpsys の書式違い) | 再現 + 確認 |
| F20 | 中 | `run --failed` の「前回」がプロファイル・プラットフォーム・**機械**を見ない(リモートで落ちた分を再実行しない) | 再現 |
| F26 | 中 | insights の所要退行・連続失敗がシナリオ ID だけで束ねる = プラットフォーム・エンジン・機械を混ぜる | 再現 + 確認 |
| F31 | 中(性能) | XCUITest エンジンで `scrollToTop` の端の判定が収束せず**スワイプ上限 20 まで払い切る**(E2E-iOS の S0010 scene 17 で 17 回中 9 回・約 32 秒。実際は端に着いている) | 9月の結果の集計 |
| F8 | 中(E2E 資産) | 物理 Pixel 4a で E2E-CMP 3本赤(キーボードで窓が縮み、下のボタンが木から消える)= 規律⑤違反 | 再現 |
| F19 | 小〜中 | 供給段でエラー終了した run が `finishedAt` 無し = 「クラッシュか強制終了」に数えられ、割り当て分の記録が0件 | 再現 |
| F15 | 小〜中 | 失敗でスキップされたアサーションを「アサーションが無い」と誤 lint | 再現 |

環境の事象(ツールの欠陥ではない)は §15。

---

## 1. Android: 容器タップ → `type` が前に焦点のあった別の欄へ入って緑になる【大】(F13)

**再現**(物理 Pixel 3a・Android 12・`com.ftester.e2e.android`。`ft_batch` は StepExecutor = シナリオと同じ経路):

```
ft_batch  tap "#nav_input"
ft_batch  tap "#field_single"; type "kb"      → single=kb
ft_batch  tap "#field_wrapped"; type "W3"     → 1. tap — ok / 2. type — ok / All 2 step(s) passed
木:       single=kbW3 / wrapped=(空)          ← 入り先が違う
```

焦点がどこにも無い状態(起動直後)から同じことをすると救済が働き `wrapped=W4` になる(対照)。
**セレクタ付きの形 `type("#field_wrapped", "W5")` は正しく入る**(注記付き。エミュレータで確認)= 誤るのは `tap(容器)` と別行の `type(文字)` の組だけ。
**エミュレータ(Pixel_9_Android_15_-02)でも同じく再現**(物理端末固有ではない)。

**素性**(確認): 救済 `retypeTargetIfUnfocused` の門は `InputFocusRescue.nothingHasFocus(snapshot.elements)`
(`StepExecutor+Actions.swift:2136`)= **木の全体で**焦点が無いときだけ救う。Android は焦点を持たない容器を
叩いても前の EditText の焦点を外さないので、門で帰り、`type` はフォーカス中の別の欄へ送られる。
読み返し(Android 注入器は `verifiesTypedText = true`)も**焦点のある別の欄**を読んで一致するので、
誤りがどこにも出ない。

**害**: CLAUDE.md が「支えるべき書き方」と定める `tap(入力欄) → type()` の Shirates 伝統形が、
**直前に別の欄へ入力していた**という普通の画面遷移で黙って誤る。witness(`E2EAppAndroid` の
`#field_wrapped`)は焦点の無い状態からしか叩かないので、E2E は緑のまま通る。

**同型**: MCP の `ft_tap(容器)` → ref なし `ft_type` も同じ(MCP 担当が 2/2 再現。
`MCPServer+Dispatch.swift` は Android の ref なし type を読み返しの対象から外しているので、どこへ入ったかも言わない)。

**直す向き**: 門を「全体に焦点が無い」ではなく「**焦点のある要素が、叩いた要素そのものでもその内側でもない**」に。
witness に「別の欄へ入力してから `#field_wrapped`」の1本を足す。

---

## 2. システムアラートに覆われた iOS 台を「画面凍結」と誤判定する【大】(F9)

**再現①**(r1 の E2E-Flutter/ios-inapp・M1Max 台 -01、2回連続): 
失敗文にツール自身が
`a system alert (「Allow “FT E2E iOS” to access your photo library?」…) is in front of the app and no iosAlertHandler is registered`
と書いた直後に `Worker … dropped out: dropped out because of a frozen screen` →
`🔧 Stopped stale bridges` → 復活 → `🔁 Re-running on another device because of a frozen screen` →
**同じ台 -01**(ポートだけ新しい)で同じ理由で赤 → 再度離脱。

**再現②**(手元で狙って、1/1): 台 -08 で写真の権限要求を出したまま残し、失敗するだけの一時シナリオ
(`launchApp()` → `exist("#this_id_does_not_exist", timeout: 2)`)を E2E-Flutter / ios-inapp で -08 に当てた。
①と同じ経過で離脱×2、最後に **-08 のブリッジ2本を止めて**終わった(一時シナリオは実行後に削除済み)。

**素性**(確認): アラートでアプリが非アクティブになると、Flutter の **in-app スクショが一様な白**になる
(失敗レポートの scene1.png・occlusion の crop・`ft_screenshot` のいずれも真っ白。
`ft_screenshot` は注記も付けずに白い画像を返す)。`FTRuntime.handleFailure` の白フレーム検査
(`BlankFrameDetector.isUniformBlank` → `markDeviceFrozen`、`FTRuntime.swift:1420-1452`)がこれを凍結と読む。
同じ失敗の中で「システムアラートが前面にある」事実を既に得ているのに、凍結の推定がそれを見ない。

**害**: ①原因が「凍結」と記録され(run 表示・`workerAnomalies`)アラートの件が埋もれる
②ブリッジの作り直し・復活・再実行で数十秒を払い、**結果は変わらない**(台を変えていない)
③「別の台で再実行」と言うが実際は同じ台(文言は `RunOrchestrator.swift:1225`。再実行先が同じ物理台かを見ていない)
④終わり際にその台のブリッジを止めるので、次の run とモニターが張り直しを払う。

**同型**: run 開始前の `BlankWorkerTriage` も in-app の白を読むが、**当てたところ凍結とは判定しなかった**
(アラートを残した Flutter 台で run を開始 → 白の判定窓で約18秒止まった後、入力で描画が戻るかを見る nudge で健全と判定。再起動なし)。
`StepExecutor+Assert.unfrozenScreenshot`(screenLooksLike)は同じ白を読む経路で未検証(手元の FM が死んでいて届かない)。

**直す向き**: システムアラートの前面(`system-alert-present` を得た/失敗文に名指しした)が分かっている間は
白フレームを凍結の根拠にしない。再実行の文言は再実行先が同じ UDID なら「同じ台で」。

---

## 3. 拡張の「テストを中断」で `api run` が即死し、録画が孤児になる【大】(F28)

**再現**(手元だけ・2台): `fleetest api run --project E2E-CMP --profile ios-inapp --runner local --set record=true --scenario …`
を、最初の `scenarioFinished` の3秒後に **pid へ SIGTERM**(拡張の中断と同じ届き方)。

- `api run` は **0 秒で rc=143**(既定の動作で終了。巻き戻しが1つも走らない)
- **`simctl io <udid> recordVideo` が5本、PPID=1 の孤児として録画を続けた**(40 秒以上。私が SIGINT で止めて確定させた)
- `recordings/index.json` が書かれない = 拡張の録画タブからこの run のクリップが見えない。切り出し前の生 `.mov` 約 50 MB だけが残る
- run.json は `finishedAt` 無し(= insights の「クラッシュか強制終了」)、NDJSON に `runFinished` も出ない
- シナリオ子(`fleetest-scenarios`)は ParentDeathWatch で消えた。`warm-ocr` は設計どおり残る(有限)

**素性**(確認): SIGTERM を受けて巻き戻すのは多機械の fanout(`ApiRunMachineFanout.swift:291`)・常駐系(`api monitor` 等)・
リモート経路で InterruptRelay が登録されている間だけ。手元だけの `api run` には受け口が無い。
一方、拡張は `proc.kill("SIGTERM")` だけを送り(`vscode-fleetest/src/cli.ts:152`)、コメントと CLAUDE.md の
「終了猶予の方針」は「`api run` は自前の後始末を持つので SIGTERM を送って待つ」を前提にしている。
多機械 profile でも fanout が手元の子 `api run` へ SIGTERM を中継するので、手元の分は同じ形になる。
拡張から起こした場合、孤児は `FT_PARENT_PID` を継ぐので**次に拡張が activate したときの掃除**で消えるが、それまでは録り続ける
(負荷とディスクを食い続ける。私の手元では `FT_PARENT_PID` 無しで起こしたので、放っておけば掃除の対象にもならなかった)。

**Android の録画も同じ**(E2E-Android で同じ中断 → ホスト側の `adb -s … shell screenrecord … /sdcard/ftrec-src-*.mp4` 5本と端末側の `screenrecord` 5本が孤児。
こちらは `--time-limit 180` が付いているので3分で自ら終わる = **上限が無いのは iOS の `simctl recordVideo` だけ**)。
**CLI の `fleetest run` も同じ**(pid への SIGTERM で rc=143・0 秒、`simctl recordVideo` の孤児 7 本。CI の中断や `kill <pid>` の形)。
終了スクリプトは控え(`.fleetest/hooks/<pid>.json`)が残り、`fleetest hooks reap` が代理実行した = この安全網は正常。

**直す向き**: 手元の `api run` にも SIGTERM/SIGINT の受け口を置き、録画の停止・index.json・lease の解放・run.json の完了・
`runFinished` を通ってから終わる(fanout と同じ `DispatchSourceSignal` の形)。

---

## 4. XCUITest エンジン: 焦点が無い `type` が緑を返してランナーを殺す【大】(F30)

**再現**(予備シミュレータ・XCUITest ブリッジ 8199・`com.ftester.e2e.ios`):

```
ft_batch  tap "#tab_home"; tap "#nav_input"; tap "#btn_input_clear"; type "zz"
→ 4. type "zz" — ok (3097ms) / All 4 step(s) passed     ← 何も入っていない
→ 直後に 8199 の LISTEN が消える(ランナー終了)
```

MCP の `ft_launch` → 焦点なしの `ft_type "x"` も `Typed: "x" (warning: nothing has input focus…)` を返した約5秒後に同じく死ぬ
(iOS 担当は 3/3。入力欄でない要素の ref を渡した `ft_type` も同じ)。
**対照**: 同じ DSL を in-app エンジン(手元 -08・CMP の SUT)で撃つと
`409: no focused input field — tap the target field first` で**正しく失敗**する。

**素性**(確認): `Runner/FleetestRunnerUITests/BridgeRouter.swift:597-602` の guard の else が、ref 無し・入力欄でない対象のとき
`app.typeText(req.text)` をそのまま撃つ。焦点が無いと XCTest が
`Failed to synthesize event: Neither element nor any descendant has keyboard focus` で失敗し、テストが Tear Down してランナーが終わる
(`.fleetest/bridge-8199.log`。`continueAfterFailure = true` はこの失敗には効かない)。

**害**: run の途中なら、そのステップは緑・以後は「ブリッジに届かない」でワーカー離脱に化け、原因(焦点の無い type)が記録に残らない。
**物理 iPhone は in-app が使えず XCUITest だけ**なので、実機ではこの経路しか無い。in-app と XCUITest で同じ DSL の合否が割れる。

---

## 5. XCUITest エンジン: パスワード欄の打ち直しで伏せ字が文字として入る【大】(I2)

**再現**(8199・SwiftUI の SUT。iOS 担当は UIKit・RN でも 3/3、`ft_type` の ref 経路でも):

```
ft_batch  tap "#btn_input_clear"; type "#field_password" "abc"; tap "#field_single"; type "#field_password" "XY"
→ 4 step とも ok / All 4 step(s) passed(注記は「追記になる」)
木:       password=XY•XY
```

**対照**: 同じ手順を in-app × CMP の SUT で撃つと `password=abcXY`(正常)。Flutter も正常(担当)。

**素性**(担当の読み・一部確認): iOS は再フォーカスした secure 欄の中身を最初の打鍵で消す。ランナーは入力前の値として
**伏せ字 `•••` をそのまま**期待値に使い(`BridgeRouter.swift:604` の `expected = normalizedValue(target) + main`)、
消えた後の値が期待の前方一致になるので `TypeReadback` が `.resend("•XY")` を返して「•」を追送する。

**害**: ログインのパスワード等を**黙って壊したまま緑**にする。物理 iPhone は XCUITest だけなので実機で必ず通る経路。

---

## 6. リモート回収後の走査が約50秒 — 手元レーンが遊ぶ【大・性能】(F10)

**再現**(時刻付きの単発ディスパッチ。E2E-CMP 1本・M1Max):

```
05:12:16.461 ⏱ Total: 8.2s                        ← リモートの run 本体
05:12:16.904 ==> collecting recordings and run logs
05:13:08.255 ==> relinked 1 report path(s)          ← 51.4 秒
05:13:09.096 ==> remote run finished (exit 0)
```

rsync 自体は 1 秒(`--dry-run --stats` で 1,896 ファイル)。残りは回収**後**の
`collectedScenarioTexts`(当月+前月の scenario JSON を**全件** `String` で読み `contains(stamp)`)で、
`saveHostFacts` と `relinkCollectedReports` が**同じ走査を2回**呼ぶ(`RemoteRunDispatcher.swift:93-94` / `:142-143` / `:573`)。
同じ走査を最適化ビルドで単独に測ると、手元の E2E-CMP は **46,945 ファイル / 342 MB / 23.6 秒**。

**害**: 複数機械の profile は「最後の機械の完了」で終わるので、この 50 秒がそのまま壁時計に乗る。
r1 の各 profile で手元の 8 レーンが `idle=60〜120s`(E2E-CMP/ios-inapp: 手元 72s・リモート 143s、
リモートの run 本体は 55s)。**履歴が増えるほど悪化**する(走査対象は2か月分の全 run)。
受け手の `run --runner` / 複数機械プロファイルも同じ経路(cliRun / apiRun の両方)。

**直す向き**: 走査を今回回収した run に限る(stamp を含む run はこのディスパッチの runID だけ。
rsync の `--itemize-changes` か、リモート側で runID を出力して受ける)。少なくとも2回を1回に。

---

## 7. 中断した複数機械 run がリモートの dispatch.lock を残す【大】(F12)

**再現**: `fleetest run --project E2E-CMP --profile ios-inapp`(3機械)を、M1Max の worker が join して
8 秒後にプロセスグループへ SIGINT(端末の Ctrl-C と同じ届き方)。1 秒で終了し、手元・リモートとも
プロセスは残らなかった。ところが**リモート2機の `<base>/.fleetest/dispatch.lock`(mkdir 型)が残り**、
保持者の pid(74216 / 74217 = 手元の死んだディスパッチ)のまま。以後のディスパッチは全て
`another dispatch is already running on this remote host (started by wave1008 (from ldipc96.local, pid 74216) …)`
で拒否され、**2周目の E2E のリモート分が全滅**(各 3〜10 秒で exit 1)。モニターの占有表示もこのロックを読む。

**交絡**: この実験は出力を時刻付けの perl へパイプしていた(同じグループなので perl も SIGINT で死に、
fleetest が SIGPIPE で巻き戻しの前に死んだ可能性がある)。docs/remote-runner.md §16.1 は
「端末の Ctrl-C はグループ全体に届くので子も自力で止まる」と書いている。**パイプ無し(素の端末の Ctrl-C)でのリモートの対照は未実施**
(ロックの解除 `remote unlock` が自動承認で止められ、以後リモートに出せなかった)。
ただし `fleetest run … | tee log` の Ctrl-C は同じ形なので、交絡を除いても利用者が踏む形ではある。

**コードで確認した経路**: ロックを外すのはリモートではなく**手元のディスパッチ側の `defer { releaseDispatchLock }`**
(`RemoteRunDispatcher.swift:57-58` / `:114-115`。ssh でロックのディレクトリを消す)。fleetest 本体は stdout/stderr の
SIGPIPE を無視していない(`signal(SIGPIPE, SIG_IGN)` / `F_SETNOSIGPIPE` は配信ヘルパーと `Shell` の子への書き込みにだけある)ので、
**出力の読み手が先に死ぬと、中断後の巻き戻しでログを1行書いた瞬間に SIGPIPE で落ち、`defer` に届かない**。
複数機械の親子(機械ごとの子の出力を親が読む)でも、親が先に終わると子が同じ形で落ちる。

**SIGPIPE の経路は手元で直接再現した**: `fleetest run … --set record=true 2>&1 | head -45`(読み手が途中で抜ける)で
fleetest は **rc=141(SIGPIPE)で即死**し、`simctl recordVideo` が7本、PPID=1 の孤児として残った(§3 と同じ残骸)。
= 交絡とみた要因は、それ自体が「fleetest は出力の読み手が死ぬと巻き戻し無しで死ぬ」という同じ欠陥の別の入口。
`fleetest run … | tee run.log` を Ctrl-C する使い方(CI のログ取りでも普通)で、手元なら録画の孤児、リモートなら dispatch.lock の残存になる。

**条件によらず残る点**: 次のディスパッチは「保持者 = この機械・その pid は死んでいる」を知り得るのに
自動では回収しない(`remote unlock` の手作業が要る)。

---

## 8. 手元の同じ台へ2つの run を同時に当てても無警告【中〜大】(F29)

**再現**: `fleetest run --project E2E-Android --profile android --runner local` と
`fleetest run --project E2E-CMP --profile android --runner local` を1秒差で起動(どちらも手元の Pixel 9 -01〜-06)。
**両方とも何も言わずに走り**、E2E-Android は 25 本中 13 本、E2E-CMP は 41 本中 16 本が赤、ワーカー離脱5台
(うち1台は「画面凍結」の誤判定)。赤は全部テストの失敗として結果 DB へ入った。

**素性**: リモートには dispatch.lock(fail fast)があるのに、手元には同じ台の二重使用を見る門が無い。
`run-<key>.lease`(pid + mtime)は既にあり、他の生きた pid が握っている台は検出できる。
保守者向けには docs/verification.md「E2E を同時に2つ走らせない」の運用注意があるだけで、受け手(2つのウィンドウ・CI と手元の併走)には届かない。

---

## 9. 未登録の権限アラートが run を跨いで残り、後続の全 SUT を汚染する【中・設計の穴】(F11)

**再現**: r1 の E2E-iOS で `16_システムアラート` S0020 が M1Max 台 -01 で赤(§15 のロケールで日本語パターンが不一致)。
写真の権限アラートは閉じられないまま残り、**次の E2E-Flutter の run の最前面にも出た**(§2 の起点)。
05:11 の実画面でもまだ残っていた(スクショで確認)。**r1 の E2E-RN / ios-inapp では同じ台 -01 の2本がアラートに覆われたまま緑**(結果 JSON の注記 `system-alert-present` だけが痕跡)。手元 -08 で**要求元アプリを `simctl terminate` しても
ホーム画面の上に残る**ことも確認した = 誰かが答えるまで、その台の後続の全 run が覆われる。

**素性**: 未登録のアラートは「止めない・閉じない」設計(CLAUDE.md §システムアラートの判定)。
`launchApp()` / `clearAppData()` は別アプリのアラートを消さない。in-app エンジンは覆われたまま背面を操作できるので、
FM が死んでいる機械(手元)ではこの状態でも**緑**になる(注記 `system-alert-present` は残る。§2 の手元対照)。

**直す向き(案)**: シナリオの終わり(特に失敗時)に、そのシナリオのアプリが出したアラートが残っていれば
「残っている」を事実として記録し、次のシナリオの開始時に**別アプリ由来**のアラートが前面にあるなら
その旨を注記で名指しする(閉じるかどうかは方針判断)。

---

## 10. iOS の Wipe Data がロケールを捨てる【中】(F7)

**再現**(使い捨てのシミュレータを作って消した): `AppleLanguages` を `ja-JP` に書く → `simctl erase` → 起動 →
`("en-US","ja-JP")`(= ホスト Mac の既定)に戻る。

**素性**(確認): `DeviceWiper.wipeOne(spec:platform:locale:…)` は Android では `AndroidDataWiper.wipeOne(locale:)` →
`-change-locale` でロケールを戻すが、**iOS 分岐(`eraseSimulator`)は `locale` を受け取らず捨てる**
(`Sources/FTAndroid/DeviceWiper.swift:64-110`)。ツールには iOS のロケールを書く経路が1つも無い
(`AppleLanguages` の grep は0件)。

**害**: 拡張の「Wipe Data」を iOS 台に使うと、その台だけ言語がホスト既定に黙って変わる。
ロケール依存のシナリオ(日本語のアラートパターン等)がその台でだけ落ちる。
M1Max の iOS 台 -01/-02 は現在 `("en-JP","ja-JP")`(ホスト既定)で、台 -01 の data は 9/10 19:59 に作り直されていた
(ツール経由かは記録から判別できない)= §15 の起点の可能性。

---

## 11. 中〜小の所見

- **F5【中】リモート実行の reportPath が手元に存在しないパスを指す**。
  中継行・`api run` の NDJSON(拡張のレーン表示 `runReducer.ts:277`)・JUnit(`collectJUnit`)が
  `<手元>/.fleetest/dispatch/<stamp>/reports/<file>` を出す。実物は `TestProjects/<p>/reports/<file>` に回収済みで、
  results JSON だけが `RemoteReportLink` で書き換わる。`RemotePathRewrite.rewrite` は workDir → 手元ルートの置換しかしない
  (`RemoteRunDispatcher.swift:695` / `:833`)。再現: r1 E2E-iOS M1Max S0020 の report 行 → `ls` で No such file、同名が reports/ にある
- **F2【中】全レーン復活不能のときの最後の発話が誤誘導**。M1Ultra の Android 4台が `FATAL | Broken AVD system path`
  で3回ずつ起動に失敗した直後、run は `no running emulator for AVD "Pixel_9_Android_15_-01" … Start one with: fleetest devices up`
  で終わる(`AndroidDeviceCatalog.swift:19`)。一部失敗は許容・全滅だけ throw する設計(`FleetOutcome.resolve`)で、
  投げられるのが**最初の解決失敗**であって復活失敗の理由ではない。併せて、決定的な FATAL を3回×4台リトライする(F3・小)
- **F19【小〜中】供給段でエラー終了した run が「クラッシュ」に数えられる**。上の M1Ultra の run
  (`20260910-195757Z-d4788479`)は run.json に `finishedAt` が無く scenarios も0件 → `results insights` が
  `incomplete run(s) (possible crash or force-quit)` に数える。割り当てられた12本は結果 DB に1件も残らない(理由はログにしか無い)
- **F20【中】`run --failed` の「前回」がプロファイル・プラットフォーム・機械を見ない**。
  記録は `.fleetest/last-results/<project>/<シナリオID>` に1つ(`LastResultsStore`)で、書くのはシナリオを走らせたプロセスだけ
  (`ScenarioHost.swift:362/522/533`)。(a) E2E-CMP の android-device で3本赤の後に iOS の XCUITest で同じ ID が緑 →
  `--profile android-device --failed` が `No scenarios failed last time` (b) r1 で M1Max で赤の E2E-iOS S0020 は手元の記録が
  9/10 の passed のまま → `--failed` が拾わない(**リモートで落ちた分は再実行されない**)。どちらも再現
- **F15【小〜中】失敗でスキップされたアサーションを「アサーションが無い」と誤 lint**。expectation に `exist` / `textIs` があるのに、
  前段の失敗でスキップされると `the expectation block … contains no assertions (it checks nothing)` と
  `this scenario contains no assertions at all` が出る(r1 M1Max の S0020・§2 の一時シナリオ)。失敗レポートを読む人を誤らせる
- **F18【小〜中】同名シミュレータの曖昧さを言わない**。`iPhone 17(iOS 27.0)` が2台(0113A6C1 / 6109860E)あり、
  `bridge up --device "iPhone 17(iOS 27.0)"` は黙って 0113A6C1 を起動した(マシンプロファイル M2Ultra.json が指すのは 6109860E)
- **F16【小】CLI の `tap --ref` が画面外の要素に ✅ を返す**。`#nav_diagnostics`(y=858・タブバーの下)を `tap --ref 14 --port 8130` →
  `✅ tap [14]` だが何も起きない(実画面で確認)。MCP の `ft_tap` は画面外を警告する
- **F17【小】`run-file` のヘルプは `--profile` と `--port` を並べるが併用はエラーで、しかも `Usage: run <options> / See 'run --help'`**
  (run-file ではなく run を案内する)
- **F1【小】`fleetest --help` の `remote` の説明が `--host dispatch`**(実フラグは `--runner`)
- **F21【小】死んだ pid の run-lease がいつまでも残る**(`.fleetest/run-00008110-….lease` は 9/7 から。pid+mtime 判定なので無害だが掃除されない。
  中断した run の lease も同様に残る)

---

## 12. 追加の中〜小

- **F25【中】非 debuggable の物理 Android で WebView の DOM が読めないとき、設計上出す警告が出ない**。
  物理 Pixel 4a(Android 13・`ro.debuggable=0`)で E2E-RN の WebView S0010 が赤(DOM 経路の検査 `placeholder=` が a11y へ黙って落ちた)。
  docs/design.md「Android の自作アプリで DOM が読めなかったときは黙らない」どおりなら stderr に理由が1回出るはずが、**ログに無い**。
  原因(確認): `WebViewDOMFallback.appDebuggableFlag` は `flags=0x…` の16進形しか読まず、Android 13 の `dumpsys package` は
  `flags=[ HAS_CODE ALLOW_CLEAR_USER_DATA ]`(テキスト形のみ)を出す → アプリの debuggable が「不明」→ 黙る
  (`WebViewDOMFallback.swift:95-106`。エミュレータの sdk35 は両形が出るので、この警告は**エミュレータでしか鳴らない**)。
  テキスト形は `DEBUGGABLE` の語の有無で読める
- **F26【中】insights の所要退行・連続失敗がシナリオ ID だけで束ねる**(`RunResultsQuery.swift:285` / `:410`)。
  プラットフォーム(E2E-CMP は同じ ID を iOS/Android で回す)・エンジン・機械・物理/仮想を混ぜて前後半を比べるので、
  今日 XCUITest で回しただけで E2E-CMP 10 本・E2E-Flutter 11 本が `duration regressed (+30〜79%)` になった。
  LPT の見積りは platform で分けている(docs/remote-runner.md §8)のと非対称
- **F27【小〜中】run.json が `--set` の上書きを記録しない**(キーは `fmSettings` のみ)。`--set scenarioTimeout=3` で打ち切った run を
  insights が `🔴 failed after 5 consecutive passes (possible regression)` と言う。`--set iosInappEngine=false` の run も
  profile `ios-inapp` の履歴に混ざる(F26 の混ざりを増やす)
- **F24【小〜中】`tapWithScrollRight("#tag_15")`(容器指定なし)の失敗文が誤誘導**。容器を省くと仕様上は画面中央基準の全画面スワイプなので
  赤自体は設計どおりだが、iOS は `If the list sits inside a half-open bottom sheet, expand the sheet first`、Android は
  `the scroll area reached its end` と言い、画面に横スクロール容器 `#carousel_tags` があるのに `scrollFrame:` / `withScrollRight(scrollFrame:)` を案内しない
  (`tapWithScroll*` 自体は `scrollFrame` を取らない)
- **F31【中・性能】XCUITest エンジンで `scrollToTop` の端の判定が収束せず、スワイプ上限まで払い切る**。
  9月の全結果(一時プロファイルを除く)を集計すると、E2E-iOS / ios-xcuitest の `S0010` scene 17「`scrollToTop` は端まで送る」が
  **17 回中 9 回**、上限 20 まで払い切って `stopped at the limit of 20 (may not have reached the edge yet)` を付ける(1回 約 32 秒)。
  直後の `existWithoutScroll("#row_01")` は通る = 実際は端に着いている。物理 SE3 でも 51 回中 1 回。
  端の判定は「整定後の署名が N 回続けて不変」か「ドライバの `reachedEdgeOnLastSwipe`」のどちらか(`StepExecutor+Actions.swift:156-201`)で、
  後者を持つ in-app(SwiftUI/RN/CMP)と Android は 0 回。`scrollToTop` の中央値も XCUITest 10.4 秒 / in-app 0.3 秒と大きく割れる。
  **別の形(観測)**: E2E-Flutter / ios-inapp の `S0020` scene 6 は上限には当たらないが、毎回 XCUITest へ退避して整定待ちの上限
  (`the screen did not settle (poll limit)`)に当たり中央値 7.3 秒(43 回)。E2E-CMP / ios-xcuitest も 49 回中 14 回が整定待ちの上限。
  **訂正の記録**: 途中の集計で `limit` の語を数え、整定待ちの上限をスワイプ上限と混ぜて「Flutter で 40/40」と書いていた(分母も数えていなかった)
- **観測(既知の範囲)**: M1Ultra の in-app で occlusion-guard が1回のアサーションに最大 22 秒(FM の直列待ち 14.4 秒 + 呼び出し p50 7.6 秒。
  `fm.gateWaitMaxMs` で確認)
- **F32【小】エミュレータ消失時の発話が「qemu の FATAL がログ末尾にある」と断定する**。run 中に -06 の qemu を `kill -9` した回、
  `the emulator's own exit reason (a qemu FATAL) is at the tail of …/Pixel_9_Android_15_-06.log` と出たが、ログに FATAL は0行で、
  末尾は Metal の command buffer エラー(端末の異常の信号ではないと分かっているもの)。読んだ人を無関係な行へ誘導する
  (再キュー自体は正常: 1本を別の台で再実行して 24/24 緑)
- **F33【小・文書】CLAUDE.md は「`api start-device` の `--device-machine` の既定は手元」と書くが、コードは同名が複数の機械にあると断る**
  (`ApiDeviceCommands.findDevice`。安全側なので直すのは文書)
- **F34【小】`type("xyz\n")` の改行がステップの表示にそのまま出て、ログ(とレポート)の行が割れる**
  (`✅ 102. [action] type "#field_single" "xyz` の次の行が `"` だけになる。E2E-Android の Enter キーのシナリオで毎回)
- **F36【小】`fleetest init --app-id` が bundle ID / パッケージ名として不正な文字を検証しない**(`--app-id "com.example.x;rm"` がそのまま
  `profiles/apps/*.json` と雛形シナリオの `@TestClass(app:)` / `appIs(...)` に入る。引数は配列で渡るので注入にはならないが、
  失敗は後段の install/launch で分かりにくい形で出る。`AndroidWebViewDOM.probeCommand` 等は同じ文字集合を検めている)
- **F22【小】`--skip-build` はソースと食い違う古いシナリオ実行バイナリを警告なしで使う**(build-fingerprint はあるのに見ない)。
  削除済みの一時シナリオが次の run に載って走った(私の手順の副作用。E2E-Flutter の results に `F9凍結誤判定` の記録が残る)
- **F23【小】稼働後に固まった XCUITest ブリッジの回復文言が「起動中のブリッジが 1416 秒応答していない」**(実際は数分前まで応答していた)。
  回復そのもの(停止 → 再起動 → 緑)は正常。固まった契機は、セッションのアプリが背面・権限アラートが前面の状態での座標タップ
  (09-06 台帳 §3.1「XCUITest の座標ジェスチャが背面でランナーを殺す」と同じ形)

---

## 13. E2E の集計

| 周 | 条件 | 結果 |
|---|---|---|
| r1(04:55〜05:10) | `e2e.sh --align`・モニター稼働のまま | 8 profile 中 2 緑。赤6は M1Ultra の Android(環境)×4・M1Max の E2E-iOS S0020(§15 ロケール)・M1Max の E2E-Flutter S0010(§2 の凍結誤判定)。手元分は全 profile 緑 |
| r2(05:16〜05:26) | `e2e.sh --align --record` | リモート分が全て dispatch.lock で即失敗(§7)。**手元分 8/8 緑・録画整合チェック 8/8 OK**(13〜22 クリップ) |
| 物理 Pixel 4a | E2E-CMP android-device | 37 本中 34 緑。赤3は下の F8(キーボードで窓が縮む) |
| 物理 Pixel 4a | E2E-Android android-device | 24/24 緑 |
| 手元 XCUITest | E2E-CMP ios-xcuitest(`--runner local`・JUnit 付き) | 37/37 緑。JUnit は 37 testcase・構造正常 |
| 物理 Pixel 4a | E2E-Flutter android-device | 26/26 緑 |
| 物理 Pixel 4a | E2E-RN android-device | 27 本中 26 緑。赤1は F25(WebView の DOM が読めず `placeholder=` の検査が落ちる。警告も出ない) |
| 手元 XCUITest | E2E-iOS / E2E-Flutter ios-xcuitest | 35/35・26/26 緑(一時シナリオ1本を除く) |
| 手元 | sut-ec-mobile ios-5 | 21 本中 20 緑。赤1は意図的に赤のカート S0020 |
| r3(06:49〜07:00) | `e2e.sh --align --android` と `--ios`(リモート分は §7 のロックで即失敗) | **手元分 8/8 緑(Android 57 本・iOS 71 本)** |
| 手元(実験) | 同じ台へ2 run 同時(F29)/ 中断(F28・F12)/ 打ち切り(`--set scenarioTimeout=3`)/ エミュレータ消失 | 意図した赤。消失の回は再キューで 24/24 緑 |

**F8【中・E2E 資産】** 物理 Pixel 4a(≈851dp)の E2E-CMP で `EnterキーでIMEアクションが発火すること.S0010` /
`ライフサイクルとプラットフォーム分岐が正しく働くこと.S0030` / `掴んだ要素の値を読んで後段で使えること.S0010` が赤。
いずれもソフトキーボードで窓が縮み `#btn_input_clear` / `#btn_input_submit` が木から消えた
(ツールの発話は正確: `the soft keyboard is up … the window has shrunk to fit above it`)。
エミュレータ(Pixel 9 ≈923dp・`show_ime_with_hard_keyboard=1` でキーボードは出る)では緑 = 画面の高さの差。
CLAUDE.md の規律⑤(最小サポート画面で対の要素が同時に載る)に照らすと、SUT の配置かシナリオの `hideKeyboard()` の問題。
android-device プロファイルは今回が初回で履歴は無い。E2E-Android(View/XML)は同じ端末で 24/24 緑。

---

## 14. 委譲した探索の報告

**表記**: 【再現】= 私が実行して確認 / 【確認】= 私がコードで確認 / 【未確認】= 担当の報告のみ(担当の試行回数を併記)。

### 14.1 CLI の境界値(24 件。ビルド・デバイス無しの形だけで叩いた)

中:
- **C1 `api results` の出力キャッシュがシナリオソースの増減を見ない**【確認】。insights の `retiredScenarios` は
  `definedScenarioClasses(of:)`(scenarios/ の走査)に依存するのに、鍵(`ResultsOutputCache.argumentsKey` + `scanFingerprint`)は
  run の stat しか見ない。シナリオを消しても次の run まで古い insights が返る。`ResultsOutputCache.swift` 冒頭の
  「厳密性の担保(近似を置かない)」と食い違う(担当はスクラッチで再現: count 75 vs `--no-cache` 76)
- **C2 `run --dry-run` が `--platform` を検証しない**【再現】。`--platform iOS` で `ios{}` / `android{}` を両方飛ばして緑
  (同じシナリオが `ios` なら 14 ステップ、`iOS` なら 10)。`RunScenarios.validate()` に platform の検査が無い
- **C3 `run` と `api run` で同じ打鍵の受理が割れる**【再現】。`--dry-run --set app=foo`(プロファイル無し)が
  `run` は exit 0(ℹ️ で無視)・`api run` は exit 64。`--dry-run --runner M1Max --profile …` も同様に割れる(担当)。
  `RunRejectionParityTests` の組に `--dry-run` が無い
- **C4 `remote status --json` の stdout の1行目が `==> host …`** で JSON として読めない【再現】(`RemoteCommands.swift:63` の
  `announce()` が `json` を見ない)
- **C5 `scenarioTimeout` の負値がどこでも弾かれず、ホストが trap する**【確認】。`ScenarioHost.swift:446` の
  `UInt64(watchdogSeconds)`。入口(プロファイル JSON・`--set scenarioTimeout=-5`・`api run --scenario-timeout -5`)は
  受理まで担当が再現、`api validate-profile` もエラー0で通す。同じ形の他の箇所は `max(0, …)` で守られている
- **C6 insights の `infrastructure-caused failure(s)` が原因を断定**【確認】(`RunResultsQuery.swift:985`・
  `ResultsCommand.swift:331`・`docs/user-docs/in_action/ci.md:75`)。CLAUDE.md「環境要因の失敗という分類は置かない」と、
  `results_analysis.md` の注記に反する。ja/en の対も要確認

小〜中・小(【未確認】・担当は再現済みと報告): `profile list` が壊れた JSON を「machine name is undecided」と言う /
ビルドしないコマンドで `failed to build the scenarios:` / run() 内の `ValidationError` がルートの Usage を出して exit 64
(`results list --since 0d`・`api results-run --run-id nosuch` 等)/ 負の `--limit` を受理 / `api results --min-runs 11` 以上で flaky が常に空
(窓 10 run が help に無い)/ `profile` の abstract に無い validate・docs/remote-runner.md:1357 の `remote status --fleet`(実装に無い)/
`api dsl-commands --category` の help に `id` 抜け / `api validate-profile` の発話(`[]` を構文エラー・型違いのキー名を出さない・警告が2回)/
dry-run で `--junit` `--device` を黙って無視 / insights の unfinishedRuns が実行中の run も「crash」と言う(§11 F19 と同じ欄)/
`api host-metrics-summary` が誤りを exit 0 / `remote status --json` の到達不可行に `"-"` / `--since` の help の既定値重複 /
`api remote-machines` と `remote machines list --json` で JSON の形が2通り / `doctor --roots-only --fm-only` で後者を黙って無視 /
`api bridge-sources`(`--digest` 無し)が平文 / 引数なしの `doctor` がブリッジを実際に停止する(help に無い。仕様か要確認)

### 14.2 MCP × 物理 Android(Pixel 3a・16 件)

- **M1【大】= §1 F13**【再現】
- **M2【中】`ft_type` の ref が入力欄でないと、ボタンを押してしまった後で失敗し「ref なしで撃ち直せ」と別の欄へ誘導**【再現】
  (送信ボタンの ref → 500 だが `submitted=-` → `submitted=` = 空のまま送信された。文言は「検索窓が本物の入力欄へ焦点を渡す形かも」。事前警告 `nonInputTypeTargetNote` は「中に入力欄が1つある容器」にしか出ない)
- **M3【中】「このセッションの操作では変えていない」注記が自分の直前の tap の後に出る**【未確認・担当 5/5、私の手順では 0/1】
  (`MCPServer+Snapshot.swift:237-246`)
- **M4【中】画面外の座標への tap / double tap / long press が done**【再現】(`ft_tap (5000,-20)`・`ft_long_press (-1,-1)`)
- **M5【中】`ft_pinch` を x/y で撃つと既定半径(短辺の 22% = 238px)ではこの実機で一切ズームしない**【未確認・3/3】
  (Android の最小スケールスパンを下回る推測。応答は「倍率が目減りするかも」としか言わない)
- **M6【中】物理端末の消灯中、MCP は画面を起こさず原因も名乗らない**【再現】(`ft_launch` が 23 秒後に
  `the app never came to the foreground`。起こす処理は run 経路の `ProfileWorkerFactory.swift:438` だけ)
- M7〜M16【小】: 消去成功でも「読み返せない」(空の EditText は value を省く)/ 「long labels are cut off」が毎回出る /
  id の無い ScrollView が木から落ちて scroll 印が出ない / 末尾で `ft_scroll_to` がボトムシートを勧める / クラッシュ注記が
  存在しない引数 `crashOnly` を案内し、数分前の別プロセスのクラッシュを引用 / 座標ジェスチャの説明が「tap(x:, y:)」固定 /
  キーボード下の欄の ref に「画面が変わった」と言う / ID なし画面で方向セレクタを候補に出さない /
  `ft_batch` の曖昧なラベル(3件一致)が警告なしで1件目を叩いて passed / 焦点救済の例示が別の欄を指す
- SUT 側: `E2EAppAndroid` はディープリンクで起動したプロセスで回転するとリンク先へ戻る(`MainActivity.kt:63-64`)/
  Pixel 3a の E2E APK が 8/26 版で古い

---

### 14.3 MCP × iOS 予備シミュレータ(XCUITest・約40分・「大」6件)

- **I1【大】= §4 F30**【再現】 / **I2【大】= §5**【再現】
- **I3【大】別 UIWindow の上部バナーの本体に触れると、木の画面が 402x180 に縮みアプリ全体が木から消える**【未確認・2/2】
  (`BridgeRouter.swift:513` の `root.frame`。否定アサーションが誤った緑になりうる。S0030 はバナーに触る前しか見ていない)。**対照: in-app エンジン(手元 -08・8131)で同じ手順を撃つと縮まない**(402x874 のまま・背面も木に居る)= XCUITest 側の問題
- **I4【大】権限アラートが出ている間にアプリの要素を ref で叩くと、XCTest が黙って「許可しない」を押し、タップも通る**【再現 1/1(手元 -08・XCUITest 8130)。担当 2/3】 —— 私の回: `/systemalert` が present:true(ボタン3つ)→ `ft_tap` ref=`#txt_build_info` → 応答は「アラートが前面・背面の木には何も届かない」→ `/systemalert` present:false・画面は `photos=denied`。未登録のアラートは「閉じない」設計(CLAUDE.md §システムアラートの判定)にも反し、権限の状態を黙って変える
  (注記は「何も届かない」と言う。ランナーは割り込み監視を持たないので XCTest 既定の処理と推測)
- **I5【大】`ft_rotate portrait` が成功を返した直後に横へ戻される**【未確認・担当は XCUITest 2/2・in-app 2/2。私の手順(in-app)では判定できず 0/1】(`BridgeClient.swift:882-897` が最初の rotate 時の向きを
  「元の向き」として控え、`MCPServer+Dispatch.swift:1007-1009` が縦へ回した直後に戻す)
- **I6【大】同じセッションで権限アラートが湧いても以降の ref tap に警告が出ない**【未確認・1/1+対照】(`memoizedScreenProbe` のメモ化)
- 中: ft_batch の途中でアプリが落ちると各手の結果が消えて 422 だけ(撃ち直しを誘う)/ クラッシュを「別のアプリが前面」と言う /
  frame の中心が容器の外の行を印も警告も無く done で空振り(DSL は正しく押し込む = MCP と DSL で判断が割れる)/
  全画面の別ウィンドウのモーダルの背面を無警告で done / キーボードで上へずれたステータスバー下の `#btn_back` を done で空振り /
  secure 欄・横向きで `focused` が申告されず誤警告と無用の救済
- 小: 画面外座標の tap が done(Android と同じ)/ `holdSeconds:-3` で XCUITest の例外が生で漏れる / ref と x/y の同時指定で黙って ref /
  「このセッションは変えていない」の誤帰属(Android の M3 と同じ)/ WebView の通常の行間に `webViewGapNote` / ピンチ(ref なし)の倍率 /
  `ft_open_url` の確認ダイアログでも「Delivered」 他

---

### 14.4 MCP × iOS in-app エンジン(手元 -07・約20分・既定エンジン側の対照)

XCUITest で見つかった9項目を in-app で当てた結果: **起きなかった** = 焦点なし type のランナー死亡(in-app は 409)/ 伏せ字の追送 /
バナーで木が縮む / 権限アラートへの自動回答。**起きた** = `ft_rotate portrait` が戻される(I5 はエンジン共通 = `BridgeClient.rotate` の
「元の向き」の控え)/ 画面外座標の tap が done(in-app では**悪化して実際に押す** = N1)/ 誤帰属の注記 / 容器外の行の空振り。

- **N1【大】座標タップが画面外・容器の外・キーボードの下にある要素を押す**【再現 1/1(私)・担当 5/5】。
  (201,900)(画面の高さ 874)→ 画面外の `#nav_diagnostics` が押されて遷移。担当はタブの下の `#nav_heal`・キーボードのバーの下の
  `#tab_home`・横向きのキー「g」の下の `#field_multiline` でも再現。`InAppBridge.swift:423-437` / `:587-597` が直近スナップショットの中で
  「点を含む最小の frame」を `accessibilityActivate` するだけで、画面内か・容器で切れていないか・他の窓に覆われていないかを見ない
- **N3【中〜大】入力欄でない ref・セレクタへの `type` / `clear` が、焦点のある別の欄に作用して成功と言う**【再現(DSL)・担当 4/4】。
  `tap "#field_single"; type "#btn_input_submit" "Q"` → ok・All passed、`single=Q`・送信ボタンは押されていない。
  Android の同じ形は「the target is a clickable, not a text field」と注記するのに、in-app は何も言わない(`InAppBridge.swift:599-605` / `:651-657`)
- **N5【中】キーボードを閉じても `keyboardShown: true` のまま**【再現】。入力画面で打鍵 → `pressEnter` で閉じた(スクショで確認)後も
  生の `/snapshot` が `keyboardShown: true, keyboardFrame: null`。DSL の `keyboardNotShown()` / `keyboardShown()` の判定と、
  キーボードを避けるスワイプ・焦点待ちの分岐が誤る(`InAppBridge.swift:261-268`。担当の読み: TextEffects 窓が閉じた後も全画面の frame で居残る)。
  E2E-iOS / E2E-CMP のシナリオに `keyboardShown` / `keyboardNotShown` は0件なので E2E では捕まらない
- N2【中】キーボード上の座標タップが空振りしても done(合成タッチはアプリの窓にしか届かない)【未確認・3/3】/
  N4【中】ref タップが権限アラートを突き抜けて背面を操作し、同じ応答の注記は「何も届かない」と言う(`ft_snapshot` 単独ではアラートの注記が出ない)【未確認・2/2】/
  N7【中】ブリッジの「activate 不発 → 合成タッチ」注記を MCP が捨てる(DSL は `driverFallback` に載せる)【未確認】/
  N8【小〜中】WebView の欄のクリアが成功しているのに 409【未確認・2/2】/ 小: キーボードの注記が入力欄を数えない・in-app のポートが落ちたときの文言が
  「XCUITest runner」と言う・`ft_long_press (-1,-1)` が done
- **F35【小】in-app ブリッジの注記が日本語のまま CLI の出力へ出る**(`activate 不発 → 合成タッチ(要素が反応しない場合は testTag 付与か engine=xcuitest を検討)`、
  `InAppBridge.swift:473`)。CLI の出力は英語のみという決定(2026-07-30)に反し、レポートにも残る
- **私の手順ミス**: 担当に -07 を専用で渡しておきながら、06:37 の SIGPIPE 実験の run(`--runner local`・台を絞らず)が -07 に乗り、
  担当の in-app ブリッジを落とした。**MCP で操作中の台にも run は無言で乗る**(F29 と同じ門の欠如の別の形)

---

## 15. 環境の事象(ツールの欠陥ではない。直すのは環境)

- **手元 Mac の FM が text / vision とも死亡**(04:53 以降ずっと。`ModelManagerError(1001)` ← `SensitiveContentAnalysisML(15)` =
  既知のフラップ(2026-07-23 に特定済みの ANE ロード失敗。09-06・09-08 の台帳にも同じ環境メモ)と同じ署名)。手元の緑は occlusion-guard・heal・triage 無しの緑。
  §2 の手元対照が緑になったのもこれ
- **M1Ultra の Android システムイメージが消えている**: `system-images/android-35/google_apis/arm64-v8a/` が無く、
  `system-images/` 配下の各 API ディレクトリの mtime が **9/10 19:10:13(JST)に一斉更新**(android-34 だけ 2023 年のまま)。
  E2E の4 AVD(`image.sysdir.1 = system-images/android-35/google_apis/arm64-v8a/`)が全て `Broken AVD system path` で起動不能 →
  **全 Android プロファイルの M1Ultra 分(毎回 7〜12 本)が未実行で赤**。ツールのコードに system-images を消す経路は無い
  (時刻は XCUITest の添付 870 GB の件の直後なので、空き容量確保の手作業と思われる)。
  **`remote status` は ✅ のまま**(AVD の実体までは見ない)。
  **09:05 にユーザーが `sdkmanager "system-images;android-35;google_apis;arm64-v8a"` で入れ直し、解決済み**
  (M1Ultra の Pixel 9 -01 が `api start-device` で起動・`ro.debuggable=1` を確認)
- **M1Max の iOS 台 -01/-02 の言語が `("en-JP","ja-JP")`**(手元は `ja-JP`、M1Max のホスト既定が en-JP)。
  同じ台で9/8 までは日本語のアラートを処理して緑だった。§10 の Wipe Data か手作業の erase で作り直された可能性。
  **戻すまで、E2E-iOS の `16_システムアラート` S0020 が M1Max に割り当てられるたびに赤になり、§9 の汚染を毎回作る**。
  **09:1x にユーザーが4台(-01〜-04)の `AppleLanguages` を `ja-JP,en-JP`・`AppleLocale` を `ja_JP` に書き、`api restart-devices` で再起動して解決済み**
  (同シナリオを M1Max -01 に当てて緑・注記 `waited-for-system-ui` を確認)。M1Ultra の6台は元から `ja-JP` が先頭
- **M1Max/M1Ultra の dispatch.lock が残っている**(§7 の私の中断実験の残骸。保持者 pid 74216/74217 は死亡済み)。
  **07:15 にユーザーが `remote unlock` で解除済み**(`your dispatch (pid 74216) is no longer running on this machine`。両機 free を確認)

---

## 16. 正常に動いたことを確かめたもの

`--failed`(1本・台数も絞る)/ `--broadcast`(8台)/ JUnit(37 testcase・構造正常)/ `api run` の NDJSON
(`recordingFinalizing` → `runFinished{runID}` の順)/ 多機械 `api run` でリモートが起動前に落ちても未実行分を失敗の
`scenarioFinished` で合成し `recordingFinalizing` は1回 / `--set iosInappEngine=false` が供給段まで効く /
`--set scenarioTimeout=3` の打ち切り(`timedOut: true`・子プロセス残らず)/ `api start-device` / `stop-device`(13 秒・1 秒・残骸なし)/ run 中のエミュレータ消失 → 再キュー(24/24 緑)/ 中断で残った終了スクリプトを `hooks reap` が代理実行 / `api live serve` の異常入力(壊れた行・未知 cmd・存在しない ref)/
`ft_list_scenarios` / `ft_dry_run` / `ft_run_scenario` / 全実行プロファイルの `api validate-profile` / `fleetest init`(空のスクラッチ・空白入りのディレクトリ名 → SPM のターゲット名は `_` に置換)/
`--performance` + `--runner local` / 1本だけの run で run-lease を1秒刻みで観測(使う2台だけが鮮度を持ち、使わない台は2秒で外れ、終了で全て外れる = ad217463 の修正どおり)/
固まった XCUITest ブリッジの自動回復 / 開始前トリアージはアラート下の白でも nudge で凍結と誤判定しない /
E2E が一度も通していない DSL(`selectWithScrollDown`+`lastElement`・`existWithScrollDown`・`tapWithScrollUp`・`home`+`tapAppIcon`・
`procedure`・`this` 系・`withoutScroll`)は iOS in-app / Android の両方で緑 / `api monitor` の観測 / 録画整合チェック 8/8 /
XCUITest エンジンの E2E-CMP 37/37・E2E-iOS 35/35・E2E-Flutter 26/26 / 物理 Pixel 4a の E2E-Android 24/24・E2E-Flutter 26/26 /
sut-ec-mobile ios-5 20/21(赤は意図的)

---

## 17. 手法のメモ

- **委譲した探索は「自分で1回再現してから本文へ」**。今回は CLI 担当 24 件・MCP(Android)16 件・MCP(iOS)約 30 件のうち、
  本文の大は全て私の再現つき。担当の報告どおりに再現しなかったもの(M3 の誤帰属は 0/1)は未確認のまま残した
- **「一時シナリオを scenarios/ に置く → 回す → 消す」の後は必ず建て直す**(`--skip-build` の run が古いバイナリで削除済みの
  シナリオを走らせた = F22)。確認は `strings <scenario binary> | grep <クラス名>` が 0 件
- **中断試験は出力をパイプしない**(読み手が同じグループで死ぬと SIGPIPE で先に落ちる = F12。欠陥として拾えたが、素の Ctrl-C の対照とは別物になる)
- **アラートを出したら必ず答えて閉じる**。要求元アプリを終了しても消えない(F11)。答えるときは要求元を前面にしてから
  (背面のまま XCUITest で座標タップするとランナーが固まる = F23 の契機)

---

## 18. 修正状況(2026-09-11 午前・未コミット)

作業ツリーに修正を入れた(**コミットしていない**)。`swift test --parallel` 5,955 件・`npm test` 1,586 件(失敗0・2スキップ)が緑。
ブリッジ版は **94**(92 → 93 は下の「検証中に見つけたもの」①、93 → 94 は N1 の後半)。拡張は 0.0.894。

### 18.1 デバイスで確かめたもの(手元の台だけ。リモート機は古いコミットのまま)

| # | 確かめ方 | 結果 |
|---|---|---|
| 全体 | 手元の 12 プロファイル(5 SUT の既定 8 + XCUITest 4)を v92 で1周 | 11/12 緑。赤は E2E-RN/ios-xcuitest の `persist99`→`prsist99` 1本 = 既知の打鍵中抜け(HEAD でも同じ文字列で再現済み・見送り済みの穴) |
| F28 | `api run --set record=true`(5 クラス)を最初の scenarioFinished の 3 秒後に SIGTERM | 5 秒で rc=1。`runFinished`・`recordingFinalizing`・run.json の `finishedAt` と `interrupted:true`・録画 index.json(9 クリップ)。録画・シナリオ子の残骸 0 |
| F12 | `fleetest run --set record=true 2>&1 \| head -30` | rc=0 で完走(以前は rc=141 即死)。録画の孤児 0 |
| F29 | E2E-Android/android の run 中に E2E-CMP/android を起動 | 2本目が6台を台名+保持者 pid で名指しして `refusing to start`(rc=1)。1本目は 24/24 緑のまま |
| F13 | E2E-Android 03 に scene 19/20(別の欄に焦点を残して容器を叩く)を追加し、エミュレータで実行 | 緑。`type-focus-recovered` が scene 18 と 20 で出た |
| F9 | -08 に写真の権限アラートを残し、失敗するだけの一時シナリオを E2E-Flutter/ios-inapp で当てた | 失敗文がアラートを名指し。「frozen screen」離脱・同じ台での再実行・ブリッジ停止は出ない。`workerAnomalies` 無し |
| F11 | 同じ台で次の run を開始 | 開始時に「alert is already on screen before the run starts … survives terminate/uninstall」を警告。失敗文も「要求元がこのアプリではない = 前の run の残り」と言う |
| F30 | XCUITest(-08・8130)で `tap #btn_input_clear; type "zz"` | 422「nothing has keyboard focus…」で失敗。6 秒後もランナーは LISTEN・`/status` 応答 |
| I2 | 同じ台で secure 欄を打ち直す | `password=XY`(伏せ字の混入なし。以前は `XY•XY`) |
| I3 | バナーの本体に触れた後の木 | 画面 402x874 のまま・アプリ本体の要素も木に居る |
| N3 | in-app(-01・8138)で `tap #field_single; type "#btn_input_submit" "Q"` | 409 で拒否。`single=` 空のまま・送信されない |
| N5 | in-app で打鍵 → `pressEnter` | `keyboardShown:false`・`keyboardFrame:null`(スクショでも閉じている) |
| F35 | in-app のタップ注記 | 英語(`activate did not fire -> synthetic touch …`) |
| F7 | 使い捨て Simulator を ja-JP にして `api wipe-device` | 消去後も `("ja-JP","en-US")` / `ja_JP`(使い捨て Simulator は削除済み) |
| N1 | in-app(-01)の `/tap` に座標(v94) | 画面外 (201,900) は 422。容器で切れた `#nav_diagnostics` の枠内 (201,866)・`#nav_heal` の枠内でタブの下 (201,845) は activate せず注記で名指し(画面はホームのまま)。キーボードの下へ押し上げられた `#tab_home` の枠内 (60,575) は 422「on the software keyboard」。見えている `#nav_scroll` (201,539) は従来どおり遷移 |

### 18.2 単体テストだけで固めたもの(デバイス未確認)

- リモート側: **F10**(回収後の走査を今回転送した分に限る)・**F5**(reportPath の書き換え)・**F20**(`--failed` の記録を (project, profile) 単位・リモートの結果も手元へ記録)・
  **F12 のロック自動回収**。リモート機は版照合がコミット単位なので、**コミットして揃えるまでデバイスでは確かめられない**
- 他: F2・F3・F15・F19・F21・F22・F23・F25・F26・F27・F32・F34・F36、CLI の C1・C3・C5 ほか、MCP の M2・M4・M6・M7・M11・M12・N7

### 18.3 残したもの

- F8(小さい画面で SUT の配置)・F31(XCUITest の `scrollToTop` の端判定)・I4(XCTest の既定のアラート処理。案のみ)・N2・M5・M9・F16・
  CLI の小項目の一部・MCP で操作中の台は F29 の門の対象外・F12 の素の Ctrl-C でのリモート対照(I6・N4・N8・F18・N3 の文言は §18.9)

### 18.4 検証中に見つけて直したもの

- **① I3 の修正が SpringBoard の画面を 3 倍に膨らませていた**: 子の窓の外接矩形を素朴に union すると、SpringBoard の木に居る画面外の窓で
  `screen` が 1206x2622 になる(`/systemui/snapshot` も同じ = システムアラートの判定の経路)。**ディスプレイに収まる子だけを足す**形に直し
  (寸法は XCUIScreen のスクショから1回だけ採る)、v92 のランナーが手元の台に載っていたので**版を 93 に上げた**。
  確認: SpringBoard 402x874・バナーの本体に触れた後もアプリの木が 402x874
- **② F9 の修正後もレポートが「frozen device display」と書いていた**: 凍結の判定は止めたが、レポートの注記は `evidenceBlank` だけを見ていた。
  判定と同じ `FrozenFrameJudgement` で書き分け、アラート中は「system alert was in front of the app … not counted as a frozen display」
  (`ScenarioReportWriterTests`。変異で落ちることを確認)。デバイスでも文言を確認
- **③ MCP: profile と udid/port/serial を併用すると udid を黙って無視し、プロファイルの台を操作していた**(HEAD から。-08 を名指しして -01 で
  アプリを起動した)。シナリオ系には `profileConflict` があるのに操作系には無かった。`call()` の入口(udid を畳む前)で名指しして断る
  (`MCPProfileExplicitTargetRefusalTests`。変異で落ちることを確認)。platform は併用可(プロファイル内の OS を選ぶ)

### 18.5 N1 の後半(同日・v94)

- **容器で切れた行**: in-app の snapshot が要素ごとに「祖先のスクロール容器で切った後に見えている範囲」を持ち
  (`InAppSnapshot.Result.clips`)、座標タップはその範囲に点が入る要素だけを候補にする。選び方は FTCore の純粋関数
  `BridgeCoordinateTapTarget.choose`(`BridgeDTO.swift`。ブリッジと共有)で、`BridgeCoordinateTapTargetTests` 7 本
  (フィルタを外す変異で 2 本落ちる)。外した要素が点の上にあったときは応答の注記で名指しする
- **キーボードの上**: `topmostHitView` はキーボードの窓を拾えなかった(実測)ので、通知由来の枠(`keyboardFrameIfVisible`)で
  先に 422 にする。in-app はキーを押せないので「撃つ先が無い」を失敗として言う(`hideKeyboard` は iOS で 501 なので案内は `pressEnter`)
- 確認: 上の表の N1 行。iOS in-app 4 プロファイル(CMP 37・iOS 35・Flutter 26・RN 27)緑。**ただし緑の E2E はこの経路を通らない**
  (シナリオに `tap(x:y:)` は0件、ホストが内部で撃つ「見えている部分の中心」への座標タップ(`StepExecutor+Actions` の visibleTapRect)も
  8〜9 月の結果に1件も無い)。**残るリスク**: ホストの容器推定とブリッジの clip が食い違うと、visibleTapRect の座標タップが
  activate されず合成タッチへ落ちる(SwiftUI では無反応)。観測例は無い
- 文書: docs/commands.md・docs/design.md §4.3 の 422・docs/user-docs/commands/tap(.md/_ja.md)

### 18.6 コミットとフリートでの確認(同日)

- 9 コミット(61dcdac3〜dafe229c)に分けて push・リモート2機を揃えた。各コミットは worktree で順に
  `swift build --build-tests` が通ることを確かめてから積んだ(run と remote は ScenarioHost/LastResultsStore が
  相互に依存するので、run 側へ寄せた)
- **フル E2E(`Scripts/e2e.sh --align`)は 10 プロファイル × 3 機すべて緑**
- **リモート側の修正を実ディスパッチで確認**(E2E-CMP 1本・M1Max -02、時刻付き。修正前と同じ手順):
  回収 → 終了が **52.5 秒 → 3.1 秒**(F10)/ reportPath が手元に実在するレポートを指す(F5)/
  `.fleetest/last-results/E2E-CMP/ios-inapp/` に記録される(F20)/ facts も更新される
- **その途中で見つけた退行(f727bcbc で修正)**: F10 の「今回転送された分だけ読む」は rsync の `--out-format=%n` の
  出力を使うが、rsync(macOS の openrsync も)は非 ASCII のファイル名を `\#203…` にエスケープして出すため、
  日本語のシナリオ名の JSON が1件も読めず、relink・facts・`--failed` の記録がリモート実行で黙って空振りしていた
  (フル E2E は緑のまま = 結果の合否に出ない)。`-8` を付け、本物の rsync に日本語名を運ばせるテストを足した

### 18.7 F12 のロック自動回収を実機で確認(M1Max)して見つけた2つの穴(d099abd1)

- **① 自動回収が、リモートでまだ走っている run のロックを外していた**: 判定は「この機械の自分の pid が死んでいるか」
  だけで、手元のディスパッチを `kill -9` した直後に次を出すと、リモートの run(全シナリオを流し続けていた)の
  ロックを外して同じ台へ2本目を当てる形だった。**外す前にランナー上でディスパッチの run が生きていないかを pgrep で見る**
  (`RemoteDispatchUnlock.guardingLiveRemoteRun`。自動回収・モニター起動時の掃除・`remote unlock` の3つが共有。
  確かめられないときも外さない)。確認: `kill -9` の2秒後に出したディスパッチは「still running on the runner (pid 305, 1555)」で断った
- **② `kill -9` で親が死ぬと、孤児の ssh がリモートの run を止めたままにしていた**: 孤児の ssh は出力の渡し先を失って
  channel を読まなくなり、リモートの `fleetest` は stdout(pty)への write で止まったまま(37 本を書き終えて
  finishedAt 無し)**11 分**。ssh を止めた瞬間に完走した。**`-tt` の ssh を `/bin/sh` の包み(`ParentBoundCommand`)に入れ**、
  親が死んだら 2 秒以内に ssh を止める。確認: `kill -9` の 2 秒後に手元の ssh が消え、5 秒後にリモートの run が
  `interrupted: true`・finishedAt 付きで終わり、次のディスパッチが自分の死んだロックを自動回収して緑
- 自動回収そのもの(死んだ pid・リモートの run も無い)は **pid 54457 / 73072 / 88245 の3回とも**
  「auto-releasing a stale dispatch lock … no longer running on this machine」で外れて緑
- テスト: 判定の分岐と本物の pgrep(`RemoteDispatchLiveRunGuardTests`)、包みを本物のプロセスで
  (`ParentBoundCommandTests`。親の死で子を止める・終了コードの素通し・terminate が子に届く)。
  見張りを外す変異・判定を素通しにする変異・base をエスケープしない変異で落ちる

### 18.8 コードレビューの残り(中)と、N3 の修正が入れていた退行(v95・未コミット)

- **N3 の修正が入れていた退行(in-app)**: 焦点の確認は「タップ直後に、受け口の枠がタップした点を含むか」だったが、
  2つの形で正しい受け口を断っていた。①**Flutter** の受け口(FlutterTextInputView / FlutterSecureTextInputView)は
  **1×1pt で欄の編集領域の左上**に置かれる(実測: 受け口 (16,310 1x1)・欄 (16,298 370x48)・叩いた中心 (201,322))ので、
  点を含むことが原理的に無く、**ref 付きの type がすべて 409 → XCUITest へ逃げていた**(Flutter の ios-inapp で
  type/clear 13 件中 10 件。修正前の r1/r2 は clear の 2 件と replace だけ = in-app では Flutter を消せない既知の設計)。
  ②**SwiftUI** は最初にキーボードが出たとき欄を避けて中身をずらす(実測: 欄が y=237 → 103)ので、判定を整定の後へ
  移した時点で E2E-iOS の「アプリで最初の type」2 件が同じく逃げた。どちらも緑のまま(XCUITest が入れる)なので合否には
  出ず、所要と「in-app で入れた」の意味だけが変わっていた。
  直し方: 比べる相手を**叩いた要素の今の枠**(AX ノードの accessibilityFrame。取れなければ snapshot の枠)にし、
  **面積の無い受け口は「その枠の中にあるか」**で見る(面積のある受け口は点のまま。枠で見ると、大きい容器を叩いたときに
  中の前の欄を叩いた先と取り違える)。判定は `FocusWait` の上限(1.5 秒)まで読み直す(`FocusWait` はブリッジから
  見えるよう `BridgeDTO.swift` へ移した。唯一の定義元のまま)。**最初に疑った「Flutter の焦点移動が非同期」は外れで、
  待ちを足しただけでは 10/13 のまま変わらなかった**(受け口の矩形を 409 の文言に出して初めて分かった)
- **N3 は hybrid(利用者の既定エンジン)では塞がっていなかった**: in-app が 409 で断ると、ホストは XCUITest へ撃ち直す。
  その XCUITest ランナーの `/type` は、ref が入力欄でないとき「どこかに焦点があるか」しか見ずに `app.typeText` を撃つので、
  **前の欄へ打って 200** を返していた(ホストは xcuitest の読み返しを信じて二重に確かめない = 誤った緑。`/clear` も同形)。
  N3 の確認はブリッジ単体の 409 までで、DSL を端から端まで通していなかった。
  確認(E2E-iOS・ios-inapp・一時シナリオ `tap #field_single; type "#btn_input_submit" "Q"` の後に欄が空であることを検証):
  修正前 = type は緑(`fell back to XCUITest`)で値の検証で落ちる / 修正後 = type が 422
  「keyboard focus did not move when the target was tapped…」で赤・`clearAppData` から始めた3回(-01×2・-02×1)とも欄は空。
  **直し方**: 入力欄でない ref に限り、タップの前に焦点を持っていた要素(ライブクエリ)を控え、タップ後も同じ要素のままで
  叩いた点を含まなければ 422(比べるのは座標ではなく要素そのもの = キーボードで中身がずれても取り違えない。入力欄の ref は
  叩けば焦点が動くので主経路にライブクエリを足さない)。**観測したが説明できていないもの**: 修正前の run の後、走った台(-01)
  ではなく -02 の欄に `Q` が残っていた(-02 は「送信が空で押された後に Q」の順)。修正後の3回では再現しない
- 直接の確認(ブリッジへ curl): Flutter で `#field_single` へ 200・値 `pos1` / パスワード欄へ 200 / パスワード欄に焦点を
  残して `#btn_input_submit` の ref へ type → 409(「receiver at (16,366 1x1), tapped 106,434」)で NEG はどこにも入らない。
  SwiftUI で最初のキーボードの type → 200・値 `above` / 送信ボタンの ref → 409・欄は `pos` のまま
- **デバイスでの確認(手元・`--runner local`)**: ios-inapp 4 プロファイル(CMP 37・iOS 35・Flutter 26・RN 27)緑。
  in-app の type/clear の XCUITest への逃げは CMP 0/18・iOS 0/17・Flutter 3/13(clear 2 + replace の消去 1 = 既知)・RN 0/11 で
  修正前(r1/r2)の形に戻った。ios-xcuitest は CMP・iOS・Flutter 緑、RN は 26/27(`hello123` → `hllo123` = 既知の打鍵の中抜け。
  同日朝の v93 でも同じ run が同じ文字列で赤 = 入力欄の主経路で、今回の分岐は通らない)
- **窓の重なり順**: 4 箇所(座標の当たり判定・合成スクリーンショット・座標操作の宛先・木の遮蔽)が
  windowLevel だけで並べていたため、同じ階層の窓の前後が渡し順で決まっていた(`visibleWindows` は keyWindow を
  先頭に置くので、同じ階層で手前の窓を奥として扱う)。`FTInAppBridge.backToFront` に寄せ、同順位は
  `UIApplication.windows` の並び(奥 → 手前)を第2鍵にした。**E2E の witness(別窓のモーダル)は `.alert + 1`
  なので同順位を通らない** = デバイスでは確かめていない
- **回収の rsync が一部失敗したとき**(終了コード ≠ 0)に、転送できた分の一覧まで捨てていた → 出力は常に返し、
  消去だけを成功時に限る(本物の rsync で一部を読めなくして確かめるテスト)
- **`InputFocusRescue` の「焦点は叩いた要素にある」判定が ref の一致を使っていた**(ref は木ごとに振り直す番号なので、
  新しい木で別の要素が同じ ref を持つと取り違える)→ id と型の一致、無ければ幾何
- **MCP の消去の文言**: 入力欄でない要素(容器の ref)で値が nil だったときも「cleared the field」と言っていた →
  入力欄のときだけ空と読む

### 18.9 残りから直したもの(I6・N4・N8・F18・N3 の文言。v96)

- **I6(MCP)**: `ft_tap` の撃つ前の探針(アラート → 覆う面 → ヒットテスト)を木の指紋で丸ごと使い回していたので、
  SpringBoard の許可アラートのように**アプリの木を1バイトも変えずに湧く面**は、次に木が変わるまで見えなかった。
  **アラートの1問だけは毎回聞く**(Simulator で 1 回 33〜38ms)。覆う面とヒットテストの使い回しは残す
- **N4(MCP)**: アラートの注記が「背面の木には何も届かない」と言っていたが、どちらのエンジンでもタップは届く ——
  in-app はアプリのプロセス内で activate / 合成タッチを撃つのでアラートを残したままアプリが反応し、XCUITest は XCTest の
  割り込み処理がアラートのボタン(拒否側)を押してから通す(I4)。タップの注記はエンジンごとに起きることを言い、
  スナップショットの注記は「利用者はどれにも届かない」と言う。**拒否には変えていない**(警告のまま。方針の判断は別)。
  デバイスでの確認(E2E-iOS・hybrid・-01): 診断画面で `#btn_request_photos` を叩いた後、木は変わらない(`photos=none`)まま
  次の `ft_tap`(`#txt_build_info`)が写真の許可アラートを題名・ボタン3つまで名指しし、in-app では背面へ届くと言った。
  `ft_snapshot` 単独は launch 直後の1回しかアラートを聞かない設計のまま(次のタップが言う)
- **N8(in-app)**: WebView の欄の `/clear` が、欄を空にしたのに 409「焦点のある入力欄が無い」を返していた。WebKit の受け口
  (WKContentView)は文書範囲を返さず(range が nil)、編集も Web プロセスで非同期に反映されるので、読み返しが減らないまま
  deleteBackward を上限(10000)まで空打ちして NO を返していた(欄が空になったのはこの空打ちの結果)。**全選択 → 削除1回**を撃ち、
  確かめは呼び手が木で行う(`StepExecutor.performClearInput` の事後検証・MCP の読み返し)。確認: ブリッジへ直接 type → clear が
  200・値が空・続けて打った `xyz` に前の値が残らない / DSL で `type` → `clearInput` → `type(replace:)` → `valueIs("abc")` が
  in-app のまま緑(XCUITest への逃げ無し)
- **F18**: 同名のシミュレータが複数あり、起動状態も OS も同じで規則(起動中 → 新しい OS)では決まらないとき、黙って1台目を
  選んでいた。**両方の UDID を名指しして断る**(`SimulatorCatalog.resolve`。起動中の1台・新しい OS を選ぶ規則は残す)。
  手元のマシンプロファイルに名前だけで iOS の台を指すエントリは0件(誤検知0)。確認: 実在の同名2台で `bridge up --device` が
  「several simulators are named … — name one by its UDID instead」で止まり、何も起動しない
- **N3 の文言(in-app)**: 断る理由を「タップした欄と別の欄」ではなく「叩いた要素に焦点が無く、別の欄にある = ref が入力欄でない
  (か、タップが届いていない)」と言う
- デバイスでの確認(手元・v96): ios-inapp 4 プロファイル(CMP 37・iOS 35・Flutter 26・RN 27)緑。type/clear の XCUITest への
  逃げは v95 と同じ(CMP 0・iOS 0・Flutter 3 = 既知の clear・RN 0)。XCUITest ランナーは版の定数だけの変更なので回していない
- **F16 は見送り**: CLI の `tap` は状態を持たない下位コマンドで、撃つ前に画面内かを確かめるには撮り直しが要り、撮り直すと
  ブリッジの ref 表が振り直されて、利用者が前の `snapshot` で見た番号の意味が変わる

