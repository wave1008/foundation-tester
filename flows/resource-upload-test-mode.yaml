name: Resource Upload Test Mode が ON であること
app: com.apple.Preferences
platform: ios
goal: 設定 > デベロッパ > Photos > Resource Upload Test Mode のスイッチが ON なら OK、OFF なら NG
generatedBy: manual (2026-07-07)
steps:
- action: scrollTo
  locator:
    id: com.apple.settings.developer
  fallbacks:
  - label: デベロッパ
  - label: Developer
  direction: up
  note: 設定トップを下へスクロールして「デベロッパ」行を探す
- action: tap
  locator:
    id: com.apple.settings.developer
  fallbacks:
  - label: デベロッパ
  - label: Developer
- action: scrollTo
  locator:
    id: PHOTOS_UPLOAD_DEVELOPER_MODE
    type: Switch
  fallbacks:
  - label: Resource Upload Test Mode
    type: Switch
  direction: up
  maxSwipes: 12
  note: デベロッパ画面を下へスクロールして Photos セクションのスイッチを探す
- assert: valueEquals
  locator:
    id: PHOTOS_UPLOAD_DEVELOPER_MODE
    type: Switch
  fallbacks:
  - label: Resource Upload Test Mode
    type: Switch
  expected: "1"
  timeout: 5
  note: スイッチの value が "1"(ON)なら OK、"0"(OFF)なら NG
