name: ログイン並列テスト 01
app: com.example.sampleapp
platform: ios
goal: メールアドレス test@example.com、パスワード password123 でログインし、ホーム画面に「ようこそ」が表示されることを確認する
generatedBy: ftester explore v0.1 (apple-fm-on-device)
steps:
- action: type
  locator:
    id: email
  fallbacks:
  - type: TextField
    index: 0
  text: test@example.com
  note: メールアドレスを入力するステップ1を実行する
- action: type
  locator:
    id: password
  fallbacks:
  - type: SecureTextField
    index: 0
  text: password123
  note: 2番目の入力欄にパスワードを入力する
- action: tap
  locator:
    id: login_btn
  fallbacks:
  - label: ログイン
  - type: Button
    index: 0
  note: ログインボタンをタップしてログイン処理を開始する
- action: tap
  locator:
    label: 今はしない
  optional: true
  note: パスワード保存ダイアログが出た場合のみ閉じる(出なければスキップ)
- assert: exists
  locator:
    id: welcome_text
  fallbacks:
  - label: ようこそ
  - type: StaticText
    index: 1
  timeout: 5
  note: 目標から自動抽出した確認(探索終了時にコード側で検証済み)
