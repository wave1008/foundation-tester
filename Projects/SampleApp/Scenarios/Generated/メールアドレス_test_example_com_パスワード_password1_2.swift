// メールアドレス_test_example_com_パスワード_password1_2.swift
// generated by ftester explore v0.1 (apple-fm-on-device)
// goal: メールアドレス test@example.com、パスワード password123 でログインし、ホーム画面に「ようこそ」が表示されることを確認する

import FTDSL

@TestClass(app: "com.example.sampleapp", platform: "ios")
class メールアドレス_test_example_com_パスワード_password1_2 {

    @Test("メールアドレス test@example.com、パスワード password123 でログインし、ホーム画面に「ようこそ」が表示されることを確認する")
    func S0010() {
        scenario {
            scene(1) {
                condition {
                    launchApp()
                }.action {
                    type("#email||.TextField", "test@example.com")  // メールアドレスを入力するステップ1を実行する
                    type("#password||.SecureTextField", "password123")  // パスワードを入力するステップ2を実行する
                    tap("#login_btn||ログイン||.Button")  // ログインボタンをタップしてログイン処理を開始する
                }.expectation {
                    exist("#welcome_text||ようこそ||.StaticText[2]")  // 目標から自動抽出した確認(探索終了時にコード側で検証済み)
                }
            }
        }
    }
}
