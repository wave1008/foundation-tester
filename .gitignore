.DS_Store
.build/
.ftester/
.swiftpm/
xcuserdata/
DerivedData/
# xcodegen が project.yml から生成する(コミットしない)
Runner/FTesterRunner.xcodeproj
reports/
results/
# ftester run-file の一時ステージ先(実行の前後で消える。残骸はコミットしない)
Projects/*/Scenarios/_runfile/
bench-results/
*.log
.vscode/

# in-app bridge のビルド生成物
InAppBridge/build/

# Claude Code のローカル設定(マシン固有・コミットしない)
.claude/settings.local.json
# clone 構成で install.sh を回すと `api ensure-settings` が書く(Bash 許可リスト。絶対パス入り)。
# 無視しないとクローンが dirty になり、次の更新が pull ガードで止まる
.claude/settings.json
